# DC Solar KC App — Session Handoff

*Last updated 2026-09-12, later still (Devon's Windows PC — **PAYROLL RUNS ARE ONE ROW PER GUSTO RECEIPT; "PAID THROUGH" COMES FROM `company_settings.payroll_through`, NOT `max(period_end)`.** Problem: Ben's off-cycle correction (period 08/31–09/13) shares its `period_end` with the next regular run, so recording the regular run through the app would have replaced it (the morning's `(company, period_end, kind)` key only fixed regular-vs-off-cycle, two corrections still collided), the Hours tab keyed runs by `period_end` so the one-person correction showed as the crew's "Run recorded", `recordPayrollRun` advanced `payroll_through` for ANY kind, and `lib/financials.ts` derived the accrual cut-off from `max(period_end)` so one person's correction marked everyone's hours paid. Fix: migration `2026-09-12_payroll_runs_receipt_key.sql` (APPLIED, proven in a rolled-back probe first: second correction + regular run coexist on one `period_end`, same-receipt re-save updates in place, receipt-less runs stay one-per-period via `nulls not distinct`, duplicate receipt rejected, outsider still sees 0 rows) replaces the key with `unique nulls not distinct (company, period_end, receipt_id)`. `lib/payroll.ts`: `fetchPayrollRuns` returns a list, `runsForPeriod(runs, period)` splits it into the period's regular run (period_end inside the period, so the 08/18–08/28 transition run now shows under Aug 18–31) + off-cycle runs; `recordPayrollRun` takes `id` (edit in place, keeps the run's own dates) or upserts on the receipt key, maps unique violations to a plain message, and advances `payroll_through` only for `kind = 'regular'`. Hours tab: Regular / Off-cycle chips on the form, off-cycle runs listed under the regular one with their own Edit, a "Record off-cycle run" button. `lib/financials.ts` reads `company_settings.payroll_through` alongside the runs and uses it as the cut-off (newest regular `period_end` only as a fallback when the marker is null) — same field `lib/cashPosition.ts` already used, so the Labor tile and "wages worked, unpaid" now agree by construction; `lib/valuation.ts` keeps its per-day paid-through from regular runs by payday (a time series, the marker is a single value). Financials month rows key on receipt and label off-cycle runs. Verified `npx tsc --noEmit` + `npx expo export --platform web`. Not yet pushed / OTA'd from this worktree branch (`claude/heuristic-murdock-8ee775`).)*

*Prior entry, 2026-09-12, late night (Devon's Windows PC — **BOOKS RECONCILED TO THE BANK + EVERY TICKER AUDITED + STOCK-STYLE CHART.** Devon supplied the Chase activity export (06/26 → 09/11) and two Gusto payroll receipts; the bank is the gold standard. NO amounts, balances or names are written here or anywhere in the repo — the full reconciliation report, backups of every table before writes, the write log and the per-month proof live in the session scratchpad (`…\scratchpad\reconcile-2026-09-12\`), and every touched `finance_entries` row carries `extracted.reconciled = "2026-09-12"` + `bank_ref = "<posting date>|<amount>|<desc prefix>"` so the work is auditable from SQL alone. **What was done to the DATA (three agents, then me):** every bank line matched to ledger rows (composite matches for split card slips, payroll lines to `payroll_runs`, the 07/09 bounced-and-re-sent direct deposits recognised as net-zero); a handful of missing bank fees / insurance / RTP fees inserted as Company-container expenses; a few amount/date/description corrections; one scanner mis-parse voided (`status='void'`, amount 0, notes — the app has no void filter yet, task chip raised); the Elevation Roofing single check re-shaped as a `splitPayment`-style pair across DC-26029 / DC-26033 exactly as Devon described (invoices agree); TWO `payroll_runs` rows added from the receipts (the regular 08/18–08/28 run paid 09/04 and Ben's off-cycle correction 08/31–09/13 paid 09/14); `company_settings` re-anchored to the 09/11 closing balance with `payroll_through = 2026-08-28`; all 15 field-app receipt rows now `paid_from_bank=false` (none appear in the bank — genuinely out of pocket, owed to Isaiah). Per-month proof: credits, debits and payroll all tie to the penny for Jun/Aug/Sep; July differs only by a Stripe fee that cancels; the bridge (profit + capital − draws vs. bank) is explained to within two card transactions the scanner booked that had not posted yet. **CODE FIXES from the audit (all libs, tsc clean):** `lib/financials.ts` month/year windows use local `todayISO()` (was UTC — wrong after 7 PM); `lib/pipeline.ts` per-job labor is LOADED (×1.10626) like the tiles; `lib/payroll.ts` `recordPayrollRun` no longer re-stamps the bank anchor (`updated_at`) and clock-in days are local; `lib/cashPosition.ts` unpaid wages include clock-in `time_entries`; `lib/receipts.ts` approval sets `paid_from_bank = !needs_reimbursed` (never did — twelve reimbursable receipts were counted as bank money); `app/financials.tsx` bank auto-adjust now = statement balance AS OF `bank_balance_as_of` ± every bank-moving row DATED after it (payments, `paid_from_bank` expenses, investments, `payroll_runs` by payday) instead of "rows created after the anchor timestamp that look like Chase". **`payroll_runs.kind`** (migration `2026-09-12_payroll_runs_kind.sql`, APPLIED): regular | off_cycle, unique (company, period_end, kind); only REGULAR runs advance "paid through" in `financials.ts` and `valuation.ts` — Ben's correction had marked the whole crew's period as paid and the next regular run would have overwritten it through the upsert. The Hours tab's Record-run form still records `regular` only; off-cycle runs are entered by SQL for now. **NEW: stock-style chart at the top of Financials** (`components/financials/StockChart.tsx`, `lib/valuation.ts`, `lib/financeRealtime.ts`, `docs/VALUATION.md`): one line at a time, toggle top-right Revenue · Profit · Value, ranges 1W/1M/3M/All from 2026-07-01, Robinhood scrubbing (press/drag → hairline, dot, date + value; headline and change follow; release returns to latest) on web (pointer) and iOS (PanResponder that refuses termination so the list cannot steal it). Series are rebuilt from rows on every load and the screen subscribes to realtime changes on `finance_entries`, `payroll_runs`, `company_settings`, `company_assets`, `company_liabilities` (all added to the `supabase_realtime` publication by migration `2026-09-12_company_assets.sql`, APPLIED, admin-only RLS proven). Revenue = Σ payments; Profit = revenue − expenses − labor (runs by payday + loaded accrual after the newest regular period); Value = cash (anchor ± dated rows, walked back for earlier days) + receivables + contracted backlog × realised avg margin + fixed assets at straight-line book value − liabilities. `components/financials/CompanyAssets.tsx` at the bottom of Financials is where Devon enters vehicles / tools / loans — until he does, Value = cash + receivables + backlog. **STILL NEEDED FROM DEVON:** the six 09/04 pay-stub PDFs (only Ben's 09/14 correction stub was supplied) and the service-role key file (`C:\Durbin Enterprises\config\secrets\supabase-service-role-keys.txt`) so stubs can be uploaded to `employee-docs`; the asset/liability entries; answers to the 13 classification questions in the scratchpad report (e.g. what check 8089 was, whether the Venmo/Zelle lines are draws or purchases, whether a Chase credit card exists, the F150 as expense vs asset, the VGC ACH); and the product questions from the audit (true calendar-year "YTD" tiles or not; DC-26017's invoice on a Pending Contract job; overlapping clock-in + manual hours for Devon; hours for 08/18–08/28 mostly unlogged vs. what was paid). Shipped: commit + OTA on runtime 4; web from main.)*
*Prior entry, 2026-09-12, night (Devon's Windows PC — **BUILD 31 CRASHED AT LAUNCH ON EVERY iPHONE; BUILD 32 IS THE FIX.** Cause (proven from the IPA, not guessed): `expo-contacts@57.0.5` (published 2026-09-11) builds an `ExpoContacts.framework` whose Mach-O carries a hard `LC_LOAD_DYLIB @rpath/Testing.framework/Testing` — Apple's Swift Testing framework, which is not on iPhones and was not embedded — so dyld aborted before any JavaScript ran (no `client_diagnostics` row, web unaffected). Apple's ITMS-90863 email about `Testing.framework` was the same defect seen from the Mac side. FIX: `package.json` → `expo.autolinking.exclude: ["expo-contacts"]` (the JS package stays so Metro resolves the lazy `require`; the native module is simply absent and `deviceContactsSupported()` reports false), the expo-contacts plugin + `NSContactsUsageDescription` removed from app.json, import-screen copy says the address-book import is off. Runtime stays **4**. Build 32 = this commit. TO RESTORE THE IMPORT LATER: wait for an expo-contacts release whose framework no longer links Testing (check with `unzip` + grep the IPA, or the ITMS mail stops), remove the exclude, re-add the plugin, bump runtime, build.)*
*Prior entry, 2026-09-12, later (Devon's Windows PC — **ROUND TWO OF FEEDBACK + THE REPO LEFT ONEDRIVE.** Six asks from Devon after seeing the overhaul, all shipped (OTA 01a095e3 on runtime 4; commit pushed right after): (1) clock-in job picker is now ONLY today's scheduled jobs (schedule rows for today ∪ `scheduled_for = today`, internal excluded) + one "Company office" chip (the `is_internal` job, resolved at runtime; a "No job" fallback only when no internal row is readable); default = first scheduled job else office; the on-clock "Working on ▾" switcher uses the same set. (2) Home header is a white surface with a five-colour hub stripe (olive gradient band gone); ClockCard off-clock = white with pipeline-blue edge + blue Clock In, on-clock = HR green ground, white text, outlined Clock Out. (3) PipelineHero on web ≥900 px renders in a centred 900:391 box, 220 px tall, on white with a blue edge (was cropped/zoomed); phone unchanged. (4) "Jobs Board" removed from the CRM hub items. (5) Phone section: root cause of "no way back" = when /phone is the FIRST screen in the stack (missed-call push, deep link, `call.tsx`'s replace) the native header has no back arrow and the app tab bar is never there; fix = `headerShown/headerBackVisible` on the root `phone` screen + a `SectionBar` inside the phone layout with ‹ Back / ‹ Home and a Home button on every tab. (6) Contacts: every row edits in place (admins): company contacts → ContactEditor sheet (+ Remove), customers → new CustomerEditor (name/phone/email/address/notes via `updateCustomer`), leads → /leads/[id], crew → own cell number only (MyCellEditor); "Messages settings" copy reworded. (7) Financials → NEW **Receivables** section after the expense ledger (`components/financials/ReceivablesLedger.tsx`, `lib/financials.ts::paymentEntries/paidYtd/recordDeposit/outstandingReceivables`): every `payment` row by month with subtotals, Bank chip for scanner rows, job chip → job, inline edit/delete, job picker incl. Company (⚠ when filed on the container), "+ Add deposit" (`status recorded`, `paid_from_bank true`), tiles Received YTD / this month / Outstanding (Σ max(invoiced−paid,0) over Pending Payment jobs — a composition of the mirror's job set and /ledger/paid's per-job paid; Devon to judge). **WORKSTATION: the repo now lives at `C:\Durbin Enterprises\dev\DC-Solar-IOS-App`.** OneDrive removed the node_modules junction and restored its cloud copy (4,720 placeholders) — junctions are not supported in synced folders — so the whole repo was robocopied out (git status identical before/after), `npm ci`, tsc + export clean, the OneDrive copy deleted. `.claude/launch.json` → `scripts/dev-web.cmd` / `dev-native.cmd`. Not device-tested; not browser-tested (needs Devon's sign-in). Also: Apple's ITMS-90863 on build 31 (`Testing.framework` not on macOS) is an Xcode-16 toolchain warning, not our code — fix is App Store Connect → Pricing and Availability → untick "Make available on Mac with Apple silicon".)*
*Prior entry, 2026-09-12 (Devon's Windows PC — **THE OVERHAUL: five hubs, white palette with colour-edged tiles, same layout for every role + admin-door alerts, CRM↔Pipeline cross-links + Jobs lens, full Gmail client, multi-employee hours, job-linked clock-in, Dropbox folder-per-job + photo mirror, iPhone contacts import + tags + many-contacts-per-customer. Built by six parallel agents on one foundation; tsc + web export clean; migrations applied + RLS proven; functions deployed.** Decisions Devon made: tab bar = Home · CRM · Pipeline · Operations · Menu; "merge CRM + pipeline" = cross-links + a Jobs board lens inside the CRM (Pipeline tab stays the crew view); palette = clean white base with vivid hub colours (CRM purple / Pipeline blue / Operations amber / HR green / Systems teal — `hubColors` in theme.ts; `cream`/`sun` kept for the login art); trading cards stay under Human Resources. **FOUNDATION:** `lib/hub.ts` (HUBS + HUB_ITEMS, `itemsIn`, `hubSections`; NO role filtering — everyone sees everything), `lib/adminGate.ts` (`isLockedFor`, `explainAdminOnly` = "Admin access needed… contact your administrator", `useAdminOnlyScreen()` mounted on 17 admin screens: alert + back/Home on deep links), `components/ui/Tile.tsx` (1.5 px colour edge in the icon colour, `compact` for desktop, `locked` = lock badge but still pressable → alert), `app/hub/[key].tsx` (HR + Systems front pages), root `_layout.tsx` navigation theme moved off cream. **HOME/MENU/TABS:** five hub tiles (2 cols phone / 5 cols desktop, compact), Systems locked for crew; Menu = hubSections with locked rows; CRM tab = workspace on web / CRM hub grid on phone; Operations = the calendar; Customers tab hidden (route kept); ClockCard smaller on desktop, job picker ALWAYS offered at clock-in (today's jobs first, then every open job) and a "Working on: DC-26019 ▾" switcher while on the clock (`lib/clock.ts::updateOpenEntryJob`; `te_update` RLS lets the owner update their own open row). **CRM↔PIPELINE:** job → customer (`JobCustomerCard`, /crm/[id]), pipeline card + board customer line → /crm/[id], customer record Jobs segment has a LIVE stage control (`updateJobStage`, WheelPickerSheet) on phone and web (`StagePillControl`), lead → converted customer's jobs + "Start a job" (job-editor `customerId`), workspace "Jobs" lens renders `PipelineBoard` (`fetchJobsBoardData`) — click a card → selects the customer; `/job/[id]?focus=hours` scrolls to Hours. **GMAIL (gmail-inbox v20, gmail.modify — Devon widened the delegation 2026-09-12):** actions list (inbox/unread/starred/sent/drafts/archive/trash/spam/all/label + q + paging) · labels · thread (text, optional sanitized html) · attachment · modify (star/read/archive/trash/label) · draft.list/get/create/update/delete/send · send · label.create; `gmail-send` v14 kept as a legacy sender for old bundles. App: /inbox = Gmail-shaped (desktop: folder rail | list with search + multi-select bulk actions | reading pane; phone: folder strip + long-press menu + purple compose FAB), /inbox/[threadId] with reply/reply-all/forward/archive/star/unread/trash/label, NEW /inbox/compose (to/cc/bcc validated, autosaves a Gmail draft 3 s after edits, Send = draft.send, Discard deletes), EmailPane reuses it. Not done: outbound attachments, HTML rendering, swipe gestures, undo. **HOURS:** per-job "DC-26###" chips in an expanded employee are links → /job/[id]?focus=hours; NEW `components/LogHoursSheet.tsx` — employee MULTI-SELECT (Everyone/Clear), job picker with search, hours presets, one Save → `addMyHoursForMany` (batch, per-person fallback + outcome list); used from the Hours tab ("Log hours" button — the tab had NO log form before) and `JobMyHours`. Root cause of "one at a time": Carson's 2026-08-25 multi-select shipped to web only, never OTA'd to phones. **DROPBOX (dropbox-sync v23, migration `2026-09-12_dropbox_job_folders.sql` APPLIED + proven):** `dropbox_job_folders` (PK job_id) + `dropbox_photo_mirrors` (admin-read only, no client writes); AFTER INSERT triggers on `jobs` (not is_internal) and `job_photos` queue a row and pg_net-POST {ensure_job_folder|mirror_photo} to the function; pg_cron `dropbox-mirror-retry` every 15 min drains queued/failed (<5 attempts); folders at `/DC Solar/Jobs/<job_number> - <customer>`, uploads mode=add+autorename (Dropbox = append-only archive; app deletes never delete there); `backfill_job_folders` / `backfill_photo_mirrors` actions for the 36 jobs / 102 photos that predate this. ⚠️ **DEVON TO DO:** the August Dropbox token has READ scopes only — tick `files.content.write` in the Dropbox app console and re-mint the refresh token (docs/DROPBOX_SETUP.md steps A–B); until then every attempt records `failed: missing_scope` and the app is unaffected; then run the two backfill actions once. **CONTACTS (expo-contacts ~57.0.5 = NATIVE → BUILD 31; migrations `2026-09-12_contacts_tags.sql` + `_contacts_crew_directory.sql` APPLIED + proven):** `contacts.customer_id/tags[]/title/source/external_id/is_primary`; `phone_directory()` returns customer_id/tags/title and now answers company MEMBERS with customers + contacts (leads + crew cells stay admin-only); NEW /contacts/import (native only: expo-contacts/legacy paged 200, search, checkboxes, tag picker, attach-to-customer, dedupe external_id → phone_e164 → insert; admins only); Phone → Contacts rewritten (tag filter chips, Import from phone, edit sheet); customer record Contacts segment (phone) / block (web) with add / attach existing / primary; `lib/contacts.ts` (helpers moved out of comms.ts, re-exported); /phone/contacts is open to crew (phone/_layout gate is role-aware: other tabs hidden + alert for crew); docs/CONTACTS.md. Follow-up: `crm_merge_customers` does not move `contacts.customer_id` yet. **WORKSTATION:** OneDrive started placeholdering `app/node_modules` (13,892 cloud reparse points → Metro drops them → `expo export` dies on `@expo/metro-runtime`); fixed by moving node_modules to `C:\Durbin Enterprises\dev\DC-Solar-IOS-App\node_modules` behind a directory JUNCTION at app/node_modules (npm ci into it; lock regenerated with npm 10 after `expo install expo-contacts`). If the repo ever moves out of OneDrive, recreate the junction or just npm ci. `.claude/launch.json` (root + repo) → `scripts/dev-web.cmd` (PowerShell execution policy blocks npx.ps1, cmd works). Test accounts' passwords were RESET and stored in `C:\Durbin Enterprises\config\secrets\test-accounts.txt`. **NOT device/browser-verified** (no iPhone; browser sign-in needs Devon to type the test password): every screen as crew (locked tiles + alert), the compose autosave, the Jobs lens, ClockCard job switcher, contacts import. SHIPPED: commit + OTA on runtime 3 (the contacts import shows "needs the latest build" until build 31), then app.json runtimeVersion → "4" + EAS build 31 (see the next entry if it exists). **LATER, SAME DAY — Dropbox is WRITABLE (Devon fixed the scope): backfill ran — 36 job folders `ready`, 102 photos `mirrored`, 0 failed. dropbox-sync v24: `ensure_job_folder` now MOVES an existing folder (`files/move_v2`) when the expected path changed; migration `2026-09-12_dropbox_rename_and_merge_contacts.sql` (APPLIED, proven rolled-back) re-queues the folder on `jobs` number/name/customer change and on a `customers.name` change (every job of that customer), and `crm_merge_customers` now moves `contacts` (primary flag yields to the kept record's) and `tasks` too, returning their counts. Not exercised against Dropbox itself: an actual folder move (only the no-op and queueing paths ran) — the first real renumber is the test; a failed move shows in `dropbox_job_folders.last_error` and the cron retries it.)*
*Prior entry, 2026-09-11 (Devon's Windows PC — **FULL REVIEW OF CARSON'S 35 COMMITS (9/6–9/8) + FIXES SHIPPED: OTA on runtime 3, web from main, migration applied, 3 functions redeployed.** Four review passes (voice/phone, CRM workspace, lead intake + notifications, core regression) over the pulled code; tsc + web export clean before and after. **THE DEVICE EVIDENCE CARSON WAS WAITING FOR was already in `client_diagnostics`: 166 `voice_register` rows, ZERO ok — every attempt on Devon's and Isaiah's phones since 9/10 failed with `Failed to initialize PushKit device token`.** ROOT CAUSE (read in the SDK source, `TwilioVoiceReactNative.m`): the native module does NOT create a `PKPushRegistry` on its own — only the exported `voice_initializePushRegistry` does, i.e. JS must call `voice.initializePushRegistry()` (1.x did it from AppDelegate; 2.x moved it to JS so Expo needs no native code). The app never called it, so iOS never delivered a device token and `register()` failed deterministically; the retry ladder just repeated it. FIXED in `lib/voice.native.ts` (`ensurePushRegistry` before `register()`, + `prepareVoiceAtLaunch()` called from `_layout.tsx` on native so the registry and invite listener exist from first render, not from Home). `TWILIO_PUSH_CREDENTIAL_SID` IS set (Devon did it), so nothing else is missing — **first thing to check after the OTA: a `voice_register ok=true` row, then call the number.** SECOND REAL BUG: `notificationRouter.ts` gated "landed" on `pathname !== '/'` — but Home (`(tabs)/index.tsx`) IS `/` (the same collision signOut.ts documents), so a tap did nothing while the person sat on Home and fired the moment they opened another tab. Now gated on the root stack's focused ROUTE NAME (`hasLanded`: not index/set-password/sign-up). Also: sign-out's three steps each get their own 4 s timeout (the shared one let the local session survive and bounce back into the tabs); `unregisterForIncomingCalls` fetches a fresh token first (the stored one lives an hour); `attempts` in the diagnostic row is now correct and a missing credential no longer writes a row per foreground; `app_version` reads the build number through `expo-application` (guarded require; `Constants.nativeBuildVersion` was empty → "1.0.0 (?)"). CRM: DetailPanel "today" was UTC (after ~7 PM KC time today's appointment/job vanished) → `todayISO()`; `loadSelected` got the same cancel guard as `loadEmail` (fast A→B click showed A's data under B); realtime `messages` events now coalesce (1.5 s trailing) instead of ~15 queries per delivery receipt. Edge functions: **gmail-send v3** (recipient display names are rebuilt + quoted — a CRLF in one could inject a Bcc: header; From name quoted; non-string cc → 400 not 500), **notify v26** (self-assignment on UPDATE no longer pushes you; reads `updated_by`), **twilio-voice-inbound v4** (`DialCallStatus=answered` treated like completed — it used to dial the cell on top of a live app call). Migration **`2026-09-11_task_ownership.sql`** (APPLIED, proven rolled-back both ways: viewer tick ok, viewer steal → 42501, viewer repoint appt → 42501, viewer delete 0 rows, admin re-own ok): BEFORE UPDATE guards on `tasks`/`lead_appointments` (non-admins cannot change created_by/company/record links — the old WITH CHECK let an assignee rewrite `created_by` then delete), `updated_by` stamped from the JWT, `quote_requests_intake()` now RAISEs a WARNING on failure (it recorded errors on a table nobody can read). NOT changed, flagged for Devon: Carson's website test lead "Carson / Testing" (9/7 8:07 PM) is still an open lead + open automation task; Turnstile keys still unset (site reports `not-configured`); crew lost the Messages tile when Phone became admin-only (comment says Devon's call — confirm); Friday/Saturday-evening leads get weekend due dates (product decision); iOS Conversation switched from inverted list to scrollToEnd (web fix applied to phone too — a realtime insert while scrolled up jumps to the bottom; not a crash). NOT device-tested here (no iPhone): CallKit ring/answer, the tap-through matrix. Local notes: this PC now has EAS login (durby) + the PAT at `C:\Durbin Enterprises\config\secrets\` — pass `-TokenFile` to query.ps1 (its default is the Mac/Carson path); `core.autocrlf=false` set on this clone. **LATER THE SAME EVENING — INCOMING CALLS CONFIRMED + OUTBOUND RINGBACK REWORK (OTA #2, twilio-voice-outbound v8).** After OTA #1 Devon's phone registered on the first try (`voice_register ok=true`, build reads `1.0.0 (30)`), and a real inbound call rang his iPhone as a proper CallKit call with ringback. OUTBOUND from the phone: three dials to a family number logged `no-answer` at 0 s; Twilio's own log (read through a throwaway `twilio-debug` function, deleted afterwards along with its `TWILIO_DEBUG_KEY`) showed the far leg rang 29 s with NO error — carrier spam screening of the new number; the phone rang once Devon had TEXTED it first. Then: 30 s of dead silence while dialling. Cause: `answerOnBridge="true"` keeps the phone's leg unanswered, ringback only arrives as carrier early media (none here), and the native SDK has no local ringback (web synthesises one in `lib/ringback.ts`). FIX: the app passes `platform=native`; `twilio-voice-outbound` dials with `answerOnBridge="false" ringTone="us"` for it (web unchanged), the row starts `ringing`, `<Number statusCallbackEvent="answered completed">` moves it to `in-progress` via ParentCallSid; `voice.native.ts` reports SDK-Connected as `ringing` for outgoing calls and exposes `sid`; `call.tsx` polls `fetchCallStatus(sid)` every 2 s while ringing → `active` when the far end answers, and names no-answer/busy when it ends unconnected. **CONFIRMED ON DEVICE (Devon, 18:53 CT): ringback is audible on the iPhone while the far end rings** (an 18-s call earlier answered too fast to judge); no-answer path logs `no-answer`. Registration is throttled to once a minute (`REGISTER_MIN_INTERVAL_MS`, OTA 01a092e5) after CallKit's active/inactive flips produced six `voice_register` rows in twenty seconds. ⚠️ **TWILIO_WEBHOOK_SECRET was echoed into a Claude session transcript on 2026-09-11** (the temporary `twilio-debug` reader returned a TwiML body containing the statusCallback URL with `?k=`). The X-Twilio-Signature gate still blocks forged requests without the auth token, so this is not an open door, but rotate when convenient: new value → edge-function secret `TWILIO_WEBHOOK_SECRET`, then update the `?k=` on the Messaging Service inbound webhook, the number's *A call comes in* webhook, and the TwiML App voice URL (docs/TWILIO_SETUP.md). The debug function and its key are deleted. Number reputation, DONE by Devon 2026-09-11 evening: freecallerregistry.com submission accepted (feedback id FCRFE09112026234406166; First Orion / Hiya / TNS each email back), and Twilio Trust Hub → Voice Integrity registration `DC Solar Voice Registration` (BU4becd2…) was already Approved on 2026-08-23 — verify the number is attached to it. Expect carrier screening to ease over the following days; a customer who never rings and never sees a missed call is this, not the app.)*
*Prior entry, 2026-09-08, midday (Carson's Windows PC — **MISSED-CALL PUSH → RECENTS; DEVON'S PHONE NOT RINGING + CRASH ON TAP: diagnostics shipped, device evidence still needed.** Audit: (1) `lib/notificationRouter.ts` routed `type:'call'` to `/messages/thread` — FIXED → `/phone/recents?segment=missed` (recents.tsx reads the param; stale "the number cannot receive calls yet" copy replaced; `RecentCall.missed` already counts inbound no-answer/busy/failed/canceled — verified in `fetchRecents`). (2) Devon's phone: **Carson is NOT in `employees`** — his iPhone rang because it is signed in as Devon (`push_tokens` shows TWO iPhones under devonsd311@gmail.com registered 03:40 and 03:41 on 2026-09-08), a legitimate binding for the identity the number rings, not a stale one; Devon's own phone therefore never completed `register()` (PushKit-token race on cold start is the leading suspect) — and Home threw the result away. FIXED: `registerForIncomingCalls` now retries (2/6/15 s), re-runs on every foreground, unregisters the previous identity on an account switch, and RECORDS every attempt. (3) The crash on tap could not be reproduced here (no iPhone). NEW **`client_diagnostics`** table (migration `2026-09-08_client_diagnostics.sql`, APPLIED; self-insert / admin-read, RLS proven both ways) + `lib/diagnostics.ts` + a root `ErrorBoundary` in `_layout.tsx` that records JS crashes (`js_error`, message + stack top) instead of dying; notification taps and voice registration/invites are recorded too, with `app_version` and `runtime` (OTA id) per row. **NEXT (needs Devon's iPhone):** install the OTA (open app twice), then: open Home → wait 30 s → run `select … from client_diagnostics order by created_at desc` — a `voice_register ok=true identity=devonsd311` row means his phone is bound; then call the number, let it ring out, tap the missed-call push from foreground/background/locked/terminated — a `js_error` row is the crash, a `notification_tap ok=true route=/phone/recents` row is success. SMS untouched. No native build. OTA runtime 3 (this commit).)*
*Prior entry, 2026-09-08, later morning (Carson's Windows PC — **INBOUND VOICE: DEVON-ONLY ROUTING via `voice_routes`; the number's webhook is LIVE.** Carson pointed +1 816-744-6473's *A call comes in* at `twilio-voice-inbound?k=…` and a real call reached it (row: "Incoming call from Carson", status completed via the cell fallback). Audit: v1 rang every owner/operator with a `staff_profiles.voice_identity` — in practice only Devon today (Isaiah/Clark have none until they fetch a voice token), but Isaiah would have joined the ring on his first Home open; the fallback was "the `owner` role's cell" (= Devon's, correct by coincidence). **v2 (deployed):** migration **`2026-09-08_voice_routes.sql`** (APPLIED) — table `voice_routes(number_e164 pk, assigned_to email, label)`, admin-only RLS (proven: viewer 0 / anon 0 / admin 1), seeded `+18167446473 → devonsd311@gmail.com`; `twilio-voice-inbound` v2 resolves the DIALED number (`To`) → that one person: app first (only when `TWILIO_PUSH_CREDENTIAL_SID` exists and they have a voice identity), then THEIR OWN cell (`staff_profiles.cell_phone_e164`, `voice_bridge_enabled`; a cell equal to a Twilio number is refused — no loop), then apology + `no-answer` + `📞 Missed call` push to that person (admins when the number has no route). Nobody else rings, ever. A second number for Isaiah = one row. No app JS changed → **no OTA, no native build**. Not yet done (needs Devon/Carson): the Apple VoIP cert → Twilio Push Credential → secret `TWILIO_PUSH_CREDENTIAL_SID` (Windows-only OpenSSL steps in docs/TWILIO_SETUP.md § 8), and the real-iPhone matrix (foreground/background/locked/terminated, answer/decline, fallback, missed) — none of it exercised here; the only production evidence is Carson's one test call which ended on Devon's cell. Suggested next test: call the number again now — expected: Devon's cell rings after ~1 s (app path skipped: no credential yet), the `messages` row goes ringing → completed/no-answer, one push to Devon only.)*
*Prior entry, 2026-09-08, morning (Carson's Windows PC — **NATIVE NOTIFICATIONS ROUTE TO THE RIGHT SCREEN + INCOMING CALLS AS REAL iPHONE CALLS (code complete; one Twilio credential away).** Full audit + contract + coverage matrix in **`docs/NOTIFICATIONS.md`**. ROOT CAUSE of "tapping a notification opens nothing": `notify` sent `{title, body, sound}` with NO `data`, and the app had NO response listener/router at all. Fixed, all JS + edge functions, **no native rebuild** (the Twilio RN SDK in build 30 already owns PushKit + CallKit natively): (1) **contract** — every push carries `data` = a target (`sms_thread | call | lead | customer | job | task | appointment`, ids only; `notify::sanitizeTarget` ↔ `lib/notificationTargets.ts`); `twilio-inbound` v13 sends the exact thread ids; leads/job/schedule/payment branches send theirs. (2) **router** `lib/notificationRouter.ts` mounted once in `_layout.tsx`: foreground/background taps + cold start (`getLastNotificationResponseAsync`), queued until the root navigator has a key, a session exists and the pathname has left `/` — so a cold-start tap survives bootstrap and a signed-out tap continues after login; each response handled once; job reminders fold in. Routes: thread → `/messages/thread`, lead → `/leads/[id]`, customer → `/crm/[id]`, job → `/job/[id]`, task/appointment → their lead/customer. (3) **coverage added**: `✅ Task for you` and `📅 Appointment for you` to the assignee (migration `2026-09-08_notify_tasks_appointments.sql`, APPLIED; insert + update-of-assignee triggers on `notify_webhook()`; self-assignment and ticking send nothing — proven), `📞 Missed call`. (4) **token hygiene**: registration no longer waits for a job to exist; sign-out deletes the device's token (+ unregisters incoming calls). (5) **incoming calls**: NEW `twilio-voice-inbound` v1 (gates 401/403 proven; logs the call as a `messages` row; rings every admin's app via `<Dial><Client>` with `displayName`/ids as Parameters so CallKit shows the customer's name; falls back to the owner's cell; then apologises, marks `no-answer`, pushes Missed call); `twilio-voice-token` v5 grants `incoming` + `push_credential_sid` when secret **`TWILIO_PUSH_CREDENTIAL_SID`** exists and reports `incoming`; app `registerForIncomingCalls()` on Home (admins), answered calls parked in `lib/incomingCall.ts` → `/call?incoming=1` (call.tsx adopts the live call). Until the credential exists: app registration is a silent no-op and calls go to the owner's cell — nothing breaks. ⚠️ **DEVON TO DO (docs/TWILIO_SETUP.md § 8):** Apple VoIP Services cert → Twilio Push Credential (`CR…`) → secret `TWILIO_PUSH_CREDENTIAL_SID`; AND point the number's *A call comes in* webhook at `twilio-voice-inbound?k=…` (today callers still get Twilio's DEMO greeting — this step alone fixes that). Isaiah needs one Home open on build 30. **NOT device-tested here** (no iPhone on this PC): tap-through on foreground/background/terminated, CallKit ring/answer/decline — verified instead: `tsc`, web export, function gates/versions, trigger→notify payloads, TwiML/token shapes. First device test = Devon on build 30 after the OTA. Note: `app/src/app/call.tsx` + `lib/ringback.ts` (PulseRing + ringback from another session's working tree) were committed together with this because call.tsx had to change on top of them. Deployed: notify v24, twilio-inbound v13, twilio-voice-token v5, twilio-voice-inbound v1; OTA runtime 3; web from main.)*
*Prior entry, 2026-09-08, ~01:30 (Carson's Windows PC — **CRM Phase 10 FIRST AUTOMATIONS shipped: automatic lead → follow-up task + admin push.** Migration **`2026-09-08_automation_new_lead.sql`** (APPLIED): `tasks.source_ref` + unique partial index `tasks_source_ref_uq`; trigger `leads_automation_new_lead_trg` (AFTER INSERT on `leads`, only when `source_ref` is set — typed-in leads untouched) inserts ONE task `Contact new website lead · <name>` (`created_by = automation`, `source_ref = auto:new_lead:<id>`, linked to the lead, `assigned_to` = the lead's (NULL), due 5 PM Central same day or next-day noon after 5 PM — one constant `DUE_HOUR_LOCAL`), `ON CONFLICT DO NOTHING`, exception → WARNING so the lead can never be blocked; trigger `leads_notify_trg` reuses the existing `notify_webhook()` (pg_net) and **`notify` v22** (deployed, verify_jwt still false) pushes `🌐 New website lead — <name> · <phone|email>. A follow-up task is waiting in the CRM.` to admins. App: Activity row `Task created automatically · …` with actor **Automation** (`crmWorkspace.ts`), OTA on runtime 3. **10A audit + deferred list in `docs/AUTOMATION.md`** (SMS needs a service-role path, `gmail-send` only sends as a signed-in mapped person, pg_cron is the shape for reminders/idle rules). **Tested for real:** one anon website-style insert → one lead → exactly one task (correct fields, due next-day noon because it was 7 PM Central) + one push (`net._http_response` 200 `{"sent":3}` 73 ms later — Devon/Isaiah's phones got a "New website lead: Automation Test (ignore)" push at ~7:06 PM Central, that was me); retry via `intake_lead` → duplicate, still one task; replaying the task insert → no-op; rolled-back failure simulation (`CHECK (false)` on tasks) → lead created, zero tasks, no error; CRM Activity + Tasks panel correct; test lead/task/quote deleted. Deferred (decisions, not code): appointment reminders and any customer texting/emailing, idle-estimate task (needs N), auto-assignment. **Phase 9.5 status:** website commit `86d3207` on `durby48/dcsolarkc` main; Vercel deploy was still propagating at the end of the session — check `GET https://dcsolarkc.com/api/quote` → `{"turnstile":"not-configured"}` proves the new route is live (`"enforced"` once Devon sets the keys).)*
*Prior entry, 2026-09-08, small hours (Carson's Windows PC — **Phase 9.5 WEBSITE SPAM HARDENING shipped — in the WEBSITE repo `durby48/dcsolarkc` (commit on its `main`; Vercel auto-deploys), nothing in this repo changed.** Why: every quote request ever received was a bot and Phase 9 now turns each `quote_requests` row into a lead. `app/api/quote/route.ts` now runs rate limit (unchanged, in-memory 5/10 min/IP) → honeypot (`fax_number`: off-screen, `tabindex=-1`, `aria-hidden`, `autocomplete=off`; any content rejects) → **Cloudflare Turnstile verified server-side** (`lib/turnstile.ts` → `siteverify` with `TURNSTILE_SECRET_KEY`; missing/invalid/expired/reused token or Cloudflare unreachable ⇒ reject, fail-closed) → validation → insert → email. Every rejection: one generic sentence, no row, no lead, no email, log = check + IP only. **STAGED ROLLOUT: until the secret is set the form runs on honeypot + rate limit** (production warning logged; `GET /api/quote` reports `enforced` / `not-configured`). Widget `components/Turnstile.tsx` (explicit render) mounts only when `NEXT_PUBLIC_TURNSTILE_SITE_KEY` exists; submit waits for a token. Tests `tests/quoteSpam.test.ts` (11/11 pass, site `tsc` clean). **Verified for real:** Cloudflare `siteverify` exercised with the documented test secrets (pass / invalid-input-response / timeout-or-duplicate / missing-input-response); local `next dev` with the test keys: honeypot → 400, missing token → 400, valid → 201 → exactly one `quote_requests` row → exactly one lead (trigger), 6th request → 429, then a real submission through the rendered widget in the browser → "Request received!" → one lead; all test rows deleted. ⚠️ **DEVON TO DO (production keys — do not exist yet):** dash.cloudflare.com → Turnstile → Add widget (hostnames `dcsolarkc.com`, `www.dcsolarkc.com`, Managed) → Vercel project `dcsolarkc` env `NEXT_PUBLIC_TURNSTILE_SITE_KEY` + `TURNSTILE_SECRET_KEY` (Production + Preview) → redeploy (NEXT_PUBLIC is build-time). README "Quote-form spam protection" has the steps. Until then the live site is on honeypot + rate limit. Remaining risk: a human-driven spam service passes Turnstile; the rate limiter is per serverless instance (best-effort, as before). Next: Phase 10 targeted automation (10A audit first; first candidate = follow-up task on automatic lead; no customer messaging without approval).)*
*Prior entry, 2026-09-07, night (Carson's Windows PC — **CRM Phase 9 AUTOMATIC LEAD INTAKE SHIPPED** after Devon cleared the Gmail gate (delegation now `gmail.readonly,gmail.send`; CRM email confirmed working by Devon). Migration **`2026-09-07_lead_intake.sql`** (APPLIED): `public.intake_lead(...)` — SECURITY DEFINER, postgres-owned, EXECUTE revoked from anon/authenticated (proven 42501 both) — is THE canonical intake contract; trigger `quote_requests_intake_trg` (AFTER INSERT on `quote_requests`) feeds it with `source_ref = 'website_quote:<id>'` and writes back `quote_requests.lead_id / intake_outcome / intake_at`, catching every error so the website insert never fails. Rules: same `source_ref` → `duplicate` (unique partial index `leads_source_ref_uq`); same phone/email as an open lead / closed lead / customer → STILL `created`, with cross-referencing notes on both sides (per the Phase 9 context: a matching number is not proof of the same submission); consent copied only when the form stamped it (`sms_opt_in_source = source@version`); status new, unassigned, `created_by = website`, `created_at` = submission time. Columns added: `leads.source_ref / sms_opt_in_at / sms_opt_in_source`, `customers.sms_opt_in_at`, `quote_requests.lead_id / intake_outcome / intake_at`. **No RLS policy changed**; no website change; no public endpoint (none needed until a second source exists); no native. App: lead selects + `Lead` type carry the new columns, `convertLeadToCustomer` → `createCustomerRow` carries consent onto the customer (optional fields only), Activity `Lead received · Website`, lead panel SMS-consent fact + "Received". **Historical replay: 4 examined, 0 replayed, 4 skipped — all four are bot submissions** (random-letter names/addresses/messages, dotted-Gmail, Tor-exit IPs), recorded on each row's `intake_outcome`. ⚠️ The trigger will faithfully turn the NEXT bot submission into a lead; spam filtering belongs on the website form (honeypot / Turnstile in `QuoteForm.tsx`, repo `durby48/dcsolarkc`) — Devon/Carson's call. Tested end to end (docs/LEAD_INTAKE.md §9): anon website-style insert → one normalised lead with consent; retry → duplicate; same-phone second insert → second lead + notes; anon/viewer denied; CRM Leads lens / detail / Activity / SMS composer; manual `+ Lead` unchanged; conversion carries consent; all test rows deleted; tsc + web export clean. Commit `412c47e`; OTA on runtime 3. Next: Phase 10 automation only against concrete rules; still unassigned by default.)*
*Prior entry, 2026-09-07, end of day (Carson's Windows PC — **CRM Phase 9 (automatic lead intake): AUDIT DONE, DESIGN WRITTEN, NOT BUILT — gated.** The Phase 9 context gates implementation on Devon's real mapped-mailbox Gmail verification (Phase 7), which cannot be done from this PC (no mapped mailbox, no Google Admin; the project's function-log endpoint returns nothing, so whether Devon has tried is unknown). Read-only work only: **`docs/LEAD_INTAKE.md`** holds the 16 audit answers and the proposed design. THE FINDING THAT MATTERS: the website's quote form (`durby48/dcsolarkc`, `app/api/quote/route.ts`) has been inserting into **`quote_requests` in THIS Supabase project** since July — 4 rows (2026-07-04 → 2026-09-04), all status `new`, 2 with server-derived SMS consent evidence, none linked to any lead or customer, table has a public insert policy and NO read policy, no triggers, and the ops console never reads it. Those four leads exist only as notification emails. Proposed (unbuilt): one SECURITY DEFINER `intake_lead(...)` SQL function (service role/trigger only) called by an AFTER INSERT trigger on `quote_requests` (zero marketing-site changes) and, later, by a `?k=`-gated `lead-intake` edge function for other sources; additive columns `leads.source_ref` (unique partial → idempotency), `leads.sms_opt_in_at/_source`, `quote_requests.lead_id`; dedupe rules: same source_ref → duplicate; same phone_e164/email as an OPEN lead → merge (append re-inquiry note); won/lost lead or existing customer → new lead with a note; unassigned; no RLS change on `leads`. Decisions for Carson/Devon in the doc: approve path A, auto-assignment, carry consent through conversion, replay the 4 historical rows. Google/Meta: nothing exists, nothing claimed.)*
*Prior entry, 2026-09-07, later still (Carson's Windows PC — **CRM Phase 8 desktop UX refinement shipped, commit `2b10673`, OTA on runtime 3; web deploys from main. UI only: no schema, no RLS, no native, no shared-system change.** Looked at `/workspace` at 1440 px first (the pane had been 800 px = two-column layout) and fixed what was actually wrong: **right column** — the current job was a thin "· current" tag on the first of eleven identical rows with a read-only stage; now `components/crm/workspace/CurrentJobCard.tsx` is an olive-accented card at the top with the stage pill as a LIVE CONTROL (admins; writes through `lib/jobs.ts::updateJobStage`, the Pipeline's own call, so `status`/`completed_on` follow and the Phase 4 trigger logs it into Activity — verified: DC-26025 Pending Removal → Pending Reinstall → back, both rows in `job_stage_history`, job columns identical to before), next day on site + crew live in that card, the separate "Next up" section is gone, other jobs fold behind "Show all N" (3 shown). Order for customers: Contact · Current job · Money · Tasks · Other jobs · Files. **Left column** — selected row now carries a 3-px ocean accent bar (Chatwoot's active-conversation treatment; the white-on-canvas tint alone was invisible), "+ Lead" became an icon in the search row so the lens chips no longer wrap, `/` focuses search from anywhere, ↑/↓ move the selection, Enter opens the top match, Esc clears (web-only listeners), and the last record + lens are remembered in `localStorage` (`dcsolar.crm.workspace`) so a reload lands on the same customer. **Center** — Activity has lenses (All · Texts & calls · Email · Money · Jobs · Notes · Tasks & visits, counts on the chips, only groups with rows; Twenty's per-type timeline filter as the reference) and the Email switch shows the thread count once Gmail has answered. Chatwoot's `ConversationCard.vue` and Atomic's `ContactListContent.tsx` were read for the selected/unread patterns; no code was copied (Vue / react-admin, not portable). **Tested** on a fresh tab at 1440 px: hard load restores Cromwell, no console errors beyond the expected once-per-session `gmail-inbox` 403 for the unmapped test account; customer + lead switching, search, all four lenses, SMS pane, Activity lenses, stage control, tasks, appointments, Calendar, Pipeline, Customers, Phone; tsc + web export clean. **Gmail verification status unchanged: blocked on Devon's Google Admin scope change; not claimed.** Deliberately NOT done (Carson's call): sticky/independent column scrolling is already how it works; a native date picker; an email unread badge on list rows (a Gmail call per record); SMS bubble density (shared `Conversation` component — untouched). Next: Phase 9 automatic lead intake, only after Devon's real email pass.)*
*Prior entry, 2026-09-07 late night (Carson's Windows PC — **CRM Phase 7 EMAIL shipped, commits `d252040` + `69cf996`, OTA on runtime 3; web deploys from main.** Audit first, written up in **`docs/CRM_EMAIL.md`** (read it before touching email): two mapped mailboxes only (`devonsd311@gmail.com→devon@dcsolarkc.com`, `inettleton18@gmail.com→isaiah@dcsolarkc.com`, hard-coded in the function), `gmail.readonly` only, NOTHING stored by design, one `email` column per record (14 customers, 0 leads, 0 duplicates), no send path anywhere. **Decision: no `email_threads`/`email_messages` tables.** Gmail already is the store; the CRM reads the caller's OWN mailbox live for the selected record (`{from:x to:x cc:x}` over the whole mailbox) and projects it into the Email pane + Activity. No sync engine, no second copy, no new RLS surface, no leak path beyond the existing one-caller-one-mailbox map. **Functions:** `gmail-inbox` v8 (additive, still readonly: `label:'ALL'`, `sent`/`rfcMessageId`/`inReplyTo`/`references` on thread messages); **`gmail-send` v1 NEW** (verify_jwt true, same three gates, same `MAILBOXES` map — keep the two copies identical — token for `gmail.send` ONLY; RFC 5322 text/plain, `threadId` + In-Reply-To/References so replies file in the thread and in Sent). ⚠️ **DEVON TO DO (2 min, Google Admin):** Domain-wide delegation → client id `105976483744924526112` → scopes `gmail.readonly,gmail.send`. Until then `gmail-send` answers `503 scope_missing` and the app's Send button shows that sentence; reading works today. No redeploy needed after. **App:** `lib/crmEmail.ts` (`fetchRecordEmailThreads`, no_mailbox remembered per session), `lib/gmail.ts::sendEmail`, `components/crm/workspace/EmailPane.tsx` (threads → thread with plain-text bodies, attachment download, Open in Gmail, reply box → compose; honest states: no address on record / no mailbox for this account / Gmail unavailable / sending not enabled), Activity rows `Email received · subj` / `Email reply received · …` / `Email sent · …` (snippet only, never the body) that open the thread. **Tested:** fresh-tab hard load of `/workspace` clean; test-operator (admin, NOT mapped) sees "No mailbox is linked to your account" (the function's 403 `no_mailbox`, which the browser logs as a 403 resource error — expected); George Janus (no email) shows "No email address on this lead"; SMS conversation, Calendar, Pipeline, Customers, Phone all render; `gmail-send` OPTIONS→200, anonymous POST→401; tsc + web export clean. ⚠️ **NOT exercised: a real mapped mailbox.** This PC has no `gmail-sa-*.json` and no Devon session, so the Gmail read (thread list for a customer, thread bodies, attachments) and the send/reply path were NOT run against Gmail — Devon signing in at app.dcsolarkc.com → CRM → Cromwell → Email is the first real test; if the query returns nothing, the thing to check first is the `{from: to: cc:}` group syntax in `lib/crmEmail.ts`. Product decisions flagged in the doc, not guessed: team visibility across mailboxes (today each admin sees only their own), several addresses per customer, a shared sending identity, attachments on send (text-only today), HTML rendering (text on purpose). Next: Phase 8 UX refinement (after Devon's first real email pass), then 9 lead intake, 10 automation.)*
*Prior entry, 2026-09-07 night (Carson's Windows PC — **CRM Phases 4–6 shipped on top of the Phase 1–3 workspace, per the continuation context; nothing rebuilt, all additive; commits `d92780a` (4), `b637007` (5), `287a105` (6); one OTA on runtime 3 carrying all three; web deploys from main.** **Phase 4 — stage/status history:** migration `2026-09-07_stage_history.sql` (APPLIED) adds `job_stage_history` + `lead_status_history`, written ONLY by SECURITY DEFINER triggers on `jobs.stage` / `leads.status` (app, ops console and edge functions all get logged; `changed_by` = JWT email, NULL for service-role → shown as "system"); read policy is `exists(select 1 from jobs/leads …)` so visibility equals the parent row's — no client write policy at all. Proven by impersonation: admin reads, no-op update logs nothing, client insert refused (42501), viewer sees job history but only own-lead history, anon nothing. `composeActivity()` takes `history` and renders "J-1042 · Pending Estimate → Pending Contract · Devon"; insert rows (from NULL) skipped because "Job created" already stands. No backfill (would be fiction). **Phase 5 — tasks:** migration `2026-09-07_tasks.sql` (APPLIED): `tasks` (title, notes, nullable `due_at`, `done_at` timestamp, `assigned_to`/`created_by` as employee EMAILS — never names, customer/lead/job optional). Shape adapted from Atomic CRM (MIT, inspected: `supabase/migrations/20240730075029_init_db.sql`, `tasks/Task.tsx`, `tasksPredicate.ts`, `TasksListByDueDate.tsx`) — `done_at` not a boolean, postpone Tomorrow/Next week counted from NOW, Overdue/Today/Tomorrow/This week/Later buckets (+ our "No date"). RLS is OURS, not theirs (theirs is any-authenticated-user-anything): admins read all; everyone else only tasks assigned to or created by them (so a task about a lead cannot leak a lead the crew cannot see); insert must be signed with own email; update admin/assignee/creator; delete admin/creator. Proven both ways. App: `lib/tasks.ts`, `components/crm/workspace/{TaskItem,TaskComposer,TasksPane}.tsx`; a **Tasks** lens chip in the workspace list (badge = overdue-or-today count, one `fetchTasks({all})` read that the detail panel filters); open tasks + inline add in DetailPanel; `task_added`/`task_done` rows in Activity. Dates are typed (YYYY-MM-DD) or quick-picked — no native picker yet. **Phase 6 — lead appointments:** migration `2026-09-07_lead_appointments.sql` (APPLIED): `lead_appointments` — a SIBLING of `job_schedule_dates`, deliberately not a relaxation of it (that table is job-keyed and every calendar reader joins it to `jobs`): kind (site_visit/consultation/call/follow_up), `appt_date`, optional `start_time`, `assigned_to`, note, `outcome`. RLS mirrors leads (EXISTS on leads → `leads_own_select` decides for reps) plus "sent to me"; proven both ways. App: `lib/leadAppointments.ts`, `components/crm/workspace/Appointments.tsx` (list + outcome chips + composer) in the lead's DetailPanel ("Next up" for customers is unchanged), `appointment` rows in Activity, and the **Calendar** (`(tabs)/calendar.tsx`) reads the same range from `lead_appointments` as a SECOND, separate query and renders "Site visit — George Janus — Branson Missouri · 10:00 AM / Lead · devon" after each day's jobs (week rows, month grid chips in violet, selected-day list), tapping to `/leads/[id]`. Job schedule reads/rows/types untouched — this is the only shared screen touched, and only additively. **Tested on the real web app** (test-operator session, 800-px pane = medium layout): lead status Estimating→Contacted→Estimating produced both history rows with actor + time; a task was created, showed in DetailPanel / Tasks lens (Today bucket, record link) / Activity / chip badge, ticked done, deleted; an appointment was scheduled, showed in DetailPanel, Activity and the Calendar week view beside a job, then deleted — zero rows left in `tasks`/`lead_appointments`. `tsc` + `expo export --platform web` clean after each phase. ⚠️ A "Cannot read properties of undefined (reading 'filter')" console error seen mid-session was a Fast Refresh artifact (hooks added to `CrmWorkspace` while the page was open) surfacing from the Browser pane's cumulative per-tab console buffer; a fresh tab hard-loading `/workspace` has no console errors. **Nothing native touched; no rebuild required.** Not done / next: Phase 7 email (read `lib/gmail.ts` + `supabase/functions/gmail-inbox` first, then `email_threads`/`email_messages`; keep the honest "not connected" until real), Phase 8 desktop refinement (e.g. a real date picker for tasks/appointments, `postpone` for appointments, Tasks on Home for the crew). Leftover from the earlier Phone work: two `lead_status_history` rows on George Janus from this session's test (true events, harmless).)*
*Prior entry, 2026-09-07 evening (Carson's Windows PC — **CRM TAB, web-first, Phases 1–3 of the "DC Solar CRM — Web-First Build Context"**: new sixth tab labelled **CRM** at route `/workspace` (`(tabs)/workspace.tsx`; `/crm` is taken by the redirect + `crm/[id]`), shown on web only via `href: Platform.OS === 'web' ? undefined : null` so the phone tab bar is unchanged; admin-gated. `lib/crmWorkspace.ts` = read model (customers + open leads + jobs + threads + money rollup → recency list; `composeActivity()` projects messages/notes/jobs/finance into one day-grouped timeline, no activity table). `components/crm/workspace/{CrmWorkspace,RecordList,WorkspaceCenter,ActivityTimeline,DetailPanel}.tsx`; three layouts by width (≥1100 three columns, ≥760 two, else stacked). Reuses `Conversation` for SMS/calls; Email shown as an honest "not connected yet"; Tasks/lead appointments say "later phase". One shared-code extension: `fetchThread(id, { byLead })`. Next phases per the context doc: 4 stage/status history (small tables + triggers), 5 tasks, 6 lead appointments, 7 email (design `email_threads`/`email_messages` after reading `lib/gmail.ts` + `supabase/functions/gmail-inbox`), 8 desktop refinement. Per Carson: MIT/BSD/Apache sources (Atomic CRM especially) may be adapted as real code where portable; AGPL (Twenty, Frappe) reference only. **HOTFIX same evening:** `lib/voice.web.ts`/`voice.native.ts` had a runtime `import … from './voice'` that Metro resolved to the file ITSELF (platform resolution applies to relative imports) → "Maximum call stack size exceeded" on every Phone screen on the web and in build 30's embedded bundle. Shared token fetch moved to `lib/voiceToken.ts` (no platform variants); platform files take only types from `./voice`. Shipped as commit `6b89f03` + an OTA on runtime 3, which is what repairs build-30 phones once installed. Earlier the same day: **TWILIO IS LIVE, and the PHONE SECTION shipped (Phases 1–3 of `context.md`)**. Twilio: account + number **(816) 744-6473** + Messaging Service `MGa716…` exist; A2P 10DLC campaign **Verified**; all five `TWILIO_*` secrets set; Messaging Service inbound webhook points at `twilio-inbound?k=…`; `comms_settings.sms_enabled = true`, `voice_enabled = true`. Proven both ways with real texts (Devon's cell ↔ business number). Two production bugs found and fixed under real traffic: `twilio-status` was letting out-of-order callbacks overwrite `delivered` with a late `queued` (rank guard + one retry on a not-yet-stamped SID, `supabase/functions/twilio-status`), and `employees.is_test` now hides the two test accounts from every roster/picker. **Phone section:** Home/Menu tile **Phone** (admin, replaces the Messages tile) → `/phone` nested Tabs: Contacts · Keypad · Recents · Messages. Migration `2026-09-06_phone_contacts.sql` (APPLIED + RLS proven by impersonation: admin sees 26 directory rows, the test viewer 0, anon cannot execute) adds `contacts` (suppliers/vendors, member read / admin write), `messages.contact_id`, and `phone_directory()` (SECURITY DEFINER, re-checks `is_company_admin()`, unions customers/leads/crew/contacts, de-dupes by handset). Edge functions redeployed: `twilio-inbound` v9 (contact lookup after customer+lead), `twilio-send-sms` v10 (`contactId`, **outbound MMS: client passes storage PATHS under `job-photos/mms/`, function signs them server-side, ≤10, refuses anything else**), `twilio-call` v9 (`contactId`, files calls under a supplier). App: `components/comms/Inbox.tsx` is THE inbox (rendered by `/crm/inbox` and `/phone/messages`; deleting it breaks both — verified) and `components/comms/Conversation.tsx` is THE chat renderer (rendered by `/messages/thread` — every inbox row, keypad "Text", Contacts "Text" and the New Message picker `/messages/compose` land there — and by the customer record's Comms segment). Messages flow is phone-style: threads → tap → full-screen conversation, pencil bottom-right for a new one. **In-app calling (browser) built 2026-09-06 evening:** `/call` is the active-call screen (Mute / Keypad / End / timer); on the WEB `lib/voice.web.ts` places the call itself with `@twilio/voice-sdk` (new JS-only dep, lock regenerated with npm 10) — token from edge fn `twilio-voice-token` (verify_jwt true, admin re-check, mints a Voice Access Token by hand: HS256, `cty: twilio-fpa;v=1`), TwiML from `twilio-voice-outbound` (verify_jwt false, `?k=` + signature; returns `<Dial callerId=DC Solar><Number>`; logs the row on the PARENT CallSid; `twilio-status` now also resolves rows via `ParentCallSid`). Migration `2026-09-06_voice_identity.sql` (applied) adds `staff_profiles.voice_identity` (trigger-filled slug from the email). `lib/voice.ts` is the native stub (`inAppCallingSupported() === false`) so the phone app keeps the bridge until Phase 4. ⚠️ **Not live until Devon creates a Twilio API Key (SK… + secret) and a TwiML App (AP…) whose Voice URL is `…/twilio-voice-outbound?k=<secret>` and sets `TWILIO_API_KEY_SID`, `TWILIO_API_KEY_SECRET`, `TWILIO_TWIML_APP_SID`** — steps in `docs/TWILIO_SETUP.md` § 7. Until then the call screen says so and offers the bridge. Devon's cell (+18178232944) is saved on his staff_profiles row, so the bridge works for him everywhere. **2026-09-07: Devon set the three secrets (API key `SK72fc…`, TwiML App `APcd…`), token minting verified; the web SDK had to be loaded from `dist/twilio.js` (Metro cannot evaluate its ESM entry — see `lib/voice.web.ts`). PHASE 4 BUILD 30 BUILT 2026-09-07 12:04 CDT (EAS build `8a8efd70`, build number 30, runtime 3, commit `732b83b`; Devon approved the runtime bump), auto-submit queued as submission `3b33fa65`: `@twilio/voice-react-native-sdk@2.0.0-preview.2`, `runtimeVersion "3"`, mic usage string, `UIBackgroundModes audio+voip`; `lib/voice.native.ts` implements the same API as the web module (CallKit `contactHandle`, Speaker via `getAudioDevices`). See `app/BUILD30_CHECKLIST.md` for the on-device test list. ⚠️ Every `eas update` from this commit on targets runtime 3 — build-29 phones are frozen at group `7b6b77db` until they install build 30 from TestFlight.** ⚠️ `FlatList inverted` renders every bubble upside down on react-native-web (the old CRM Comms tab had this bug on app.dcsolarkc.com) — Conversation is oldest-first + scrollToEnd instead; do not reintroduce `inverted` for web. `components/phone/Dialpad.tsx`, `lib/comms.ts` gained `fetchDirectory`/`matchDirectory`/`createContact`/`archiveContact`/`fetchRecents`/`uploadMmsAttachment`/`signMediaPaths`; the customer Comms composer attaches pictures (compressed, uploaded on pick, sent as paths). Recents "Missed" is honest: no inbound calls exist until Phase 4, so it shows outbound calls that did not connect. **Phase 4 (true in-app VoIP) spike verdict: YELLOW** — `@twilio/voice-react-native-sdk` builds on Expo 57 / RN 0.86 only on the `2.0.0-preview.2` line, via the New-Arch interop layer (no codegen/TurboModule anywhere in it), with an open audio-clipping issue (#706) on exactly this stack and no maintainer reply; npm resolves cleanly, iOS autolinks, `expo prebuild --platform ios` cannot run on Windows so Podfile/newArch remain UNVERIFIED; app.json would need `NSMicrophoneUsageDescription`, `UIBackgroundModes: ["audio","voip"]`, `aps-environment`, a VoIP push credential — no custom `app.plugin.js` expected for 2.x. Recommendation: bridge on iOS too, revisit when 2.x goes stable; if it must ship, pin the preview and budget a device audio-quality pass. Devon still owes: his cell number in Messages settings (bridge calls need it), `comms_settings.review_link`, the supplier list (Phone → Contacts → Add a supplier), and confirmation that the 10DLC campaign permits MMS before the first picture goes to a customer. ⚠️ 4 test rows in `messages` (Devon's cell ↔ business number) still present — delete when he says so.)*
*Prior entry, 2026-08-22 (Devon's Windows PC, **2026-08 OVERHAUL — Phase 0 + Phase 1 built**; the approved ten-workstream plan lives at `C:\Users\Devon Durbin\.claude\plans\we-are-now-in-agile-otter.md` and governs everything below. SHIPPED TO MAIN THIS SESSION: (1) `supabase/migrations/2026-08-22_*` — document revisions (`revise_document` RPC, `finance_entry_revisions`, `my_documents()` v2, customers may sign their own invoice/estimate PDFs), CRM (`archived_at`, generated `phone_e164`, `customer_notes`, `crm_customer_summary`, `crm_merge_customers`), comms (`messages`, `staff_profiles`, `comms_settings`, `message_templates`, realtime), Dropbox media (`integration_secrets` zero-policy, `dropbox_folders`, `media_assets`), OAuth staff block (trigger on `auth.identities`), trading cards (`cards`/`card_sets` + private `cards` bucket, 61 cards imported from durby48/dc-solar-tcg), pg_cron daily `dropbox-sync` — ALL APPLIED + RLS proven by impersonation; (2) seven edge functions deployed (`render-document`, `twilio-send-sms`, `twilio-inbound`, `twilio-status`, `twilio-call`, `dropbox-sync`, `card-art`) — all answer 503 `not_configured` until Devon supplies credentials per `docs/TWILIO_SETUP.md`, `DROPBOX_SETUP.md`, `PDF_RENDERING_SETUP.md`; (3) app: estimates/invoices **revise in place with history** (builder revise mode, Duplicate as invoice, rev history, portal View PDF), **CRM** at `/crm` (list + `/crm/[id]` detail with Jobs/Documents/Money/Comms/Notes; `more/customers` redirects), **Messages** inbox/thread/settings, `lib/types.ts`, `jobs.ts::payloadAttempts` deleted, `app/vercel.json` (production deep links were 404ing — config lived in `public/`), `set-password` session gate + `landingRoute()`. Supabase auth config (production `site_url`, redirect allow-list incl. `dcsolarkc://**`, re-auth required for password changes, 60-day inactivity timeout, security notification emails) is LIVE — it needed a whole-project restart (Devon approved), and the restart exposed that **`sessions_inactivity_timeout` is in HOURS** (the seconds value 5184000 crash-looped GoTrue for ~15 min until it was set to 1440); verified afterwards: invite links resolve to app.dcsolarkc.com, fresh sessions can set a password, 48h-old sessions get `reauthentication_needed`. The Vercel preview host-wildcard was dropped from the allow-list during triage (plain hosts only). ⚠️ Resend domain `dcsolarkc.com` is NOT verified (no DKIM) so custom SMTP is still off (2 emails/hour). **PHASE 2 STAGED + PHASE 3 BUILT, all on `main` (web auto-deployed):** `app.json` now has **`runtimeVersion: "2"`** + every native dep (`expo-linear-gradient`, `react-native-svg`, `expo-haptics`, `expo-blur`, `expo-image-manipulator`, `expo-clipboard`, `expo-video`, `expo-apple-authentication`, `@react-native-google-signin/google-signin`, `@shopify/flash-list`, `expo-sensors`, `expo-secure-store` (unwired), Inter/Sora fonts; `expo install --fix` bumped expo 57.0.15 / RN 0.86.2) — so **every `eas update` from here targets runtime 2; build-28 phones are frozen at update group `16109a94` (hotfix them from commit `6c640ee`)**. `app/BUILD29_CHECKLIST.md` has the owner prerequisites; **social login is fully configured (2026-08-22 evening):** Google Web + iOS clients created by Devon, `external_google_*` + `external_apple_*` enabled in Supabase (live: GoTrue settings report google/apple true), the google-signin plugin is in `app.json` with the reversed iOS client id, `EXPO_PUBLIC_GOOGLE_*` ids are in `app/.env` + EAS (production) + Vercel, and the Apple secret JWT (signed with key `ZFPHJ32K5K` for Services ID `com.dcsolarkc.fieldapp.web`, `scripts/auth/apple-secret.mjs`) **expires 2027-02-18**. Secrets live in `config/secrets/` only. **LATER ON 2026-08-22 (all on main + OTA on runtime 2):** trading cards are now **per-employee decks** — `user_cards`/`card_packs`, `my_pack_status()` (1 pack per 10 h worked, mirrors `payroll.ts`, backdated: Devon 11 / Isaiah 9 / Tyler 6 / Simon 5 / Ben 4 / Garrett 3), server-side `open_card_pack()` (7 cards: 4 common, 2 uncommon, hit rare 85/legendary 12/secret 3 %, foil 10 %, holo 3 %), `my_cards()`; the `cards` table and `/cards/catalog` are **admin-only**, `/cards` is My Deck; **profile photos** (tap the Home avatar → `staff_profiles.avatar_path`, `job-photos/avatars/`); **Email** tile (admin, Work) → `/inbox` reading devon@/isaiah@dcsolarkc.com through the delegated service account (`gmail-inbox` function, secret `GMAIL_SA_JSON`, read-only); **job deletion** (`delete_job()` RPC; un-assigns money/hours, never deletes them) and **gap-filling job numbers** (next job = DC-26006 because that number never existed); Employee of the Month is a trophy placard on **Calendar only** with a photo slot for Garrett; Customers tab has Sort/Show wheel pickers, house-cartoon avatar fallback, the two insurance "customers" removed. `scripts/db/query.ps1` now reads UTF-8 (it had mangled em dashes in three function messages — re-applied). `crm/[id].tsx` header avatar now uses the house-art fallback. **Card forge (2026-08-22 night):** edge function `card-forge` (Gemini text, `GEMINI_API_KEY`) — `/cards/catalog` gets **Sync jobs → cards** (every non-internal job without a card gets one; stats fitted to the printed set, see `docs/CARD_FORGE.md`; dry-run preview; optional ~4¢ art via `card-art`; 25 per call) and **New card from a prompt** (draft → TradingCard preview → save to catalog, which puts it in the pack pool immediately, or open in the editor). 5 jobs (DC-26029..26033) currently lack cards — Devon runs the sync in the app. **`jobs.module_watts` added 2026-08-22 (migration `2026-08-22_module_watts.sql`, applied; null = 400 W, check 100–1000, DC-26019 backfilled to 600)** — job editor field under Modules / panels, card-forge (redeployed) computes `kw_dc = panels × watts ÷ 1000`, and `scripts/tcg/calibrate.mjs` (new, in-repo replacement for the scratchpad harness) confirms The Oberlin Beast reproduces at 23.4 kWdc / legendary. **BUILD 29 BUILT 2026-08-22 19:03 (EAS build `5b704562`, build number 29, runtime 2, from commit `89659ed`; Devon confirmed Sign In with Apple on the App ID first) and auto-submitted to App Store Connect (submission `429218ed`).** Once it shows in TestFlight the crew install it manually (message in `app/BUILD29_CHECKLIST.md`) and the More footer must read `runtime 2`; every OTA since `15e06c6` (latest group `dc232714`) then lands on it. Phase 3 shipped: theme foundation (`components/ui/*`, `lib/motion.ts`, `lib/haptics.ts`, olive ramp + semantic tokens, Inter/Sora via deep imports), **Home hub** (`(tabs)/index.tsx`; tabs Home · Calendar · Pipeline · Customers · Menu; `lib/hub.ts` drives Home + Menu with role gating; Employee section; ClockCard with confetti; sales/financials/hours are root routes), the signed-out redirect loop fixed (`getSession()` first + root `navigation.reset`), marketing placeholders and ALL mock data removed (`lib/mockData.ts` deleted; fetches return `{…, status}`), Google/Apple sign-up buttons (`lib/oauth.ts`, hidden until `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID` is set), **Trading Cards** (`/cards`, `/cards/[id]`, `/cards/editor`, `/cards/rules`; SVG/gradient port of the print sheet, flip + tilt, pack opening), **Dropbox photo library** (`lib/media.ts`, `/marketing-photos`, Sales → Photos, EOM "From Dropbox" picker, lightbox, `compressForUpload` on every photo upload; `deleteEmployeeOfMonth` never removes `eom/library/*`), and the Tier-2/3 restyle of every screen (financials split into `components/financials/*`). ⚠️ Never-run-on-device list: lightbox pinch/swipe, card tilt (DeviceMotion), haptics, confetti timing, Apple/Google native sign-in — first build-29 install is the real test. Open question resolved: `crew-sandals` = Garrett.)*
*Prior entry, 2026-08-05 (Mac session, PIPELINE OVERHAUL II — see [OVERHAUL.md](OVERHAUL.md). Pipeline finance totals REMOVED from the tab header (unchanged on Financials) and replaced by an animated installer plus a scrolling band of company milestones. Job cards are now TWO swipeable pages over one shared property artwork: page 1 = job/customer/address/next date, page 2 = modules, forecast hours, Est/Inv/Paid and profit %. Migration 20 adds jobs.module_count / job_type / critter_guard_panels / is_internal and customers.photo_path, all backfilled. Customer contact photos added. Property-art scrim lightened 0.80 → 0.62 — the houses were too washed out. ⚠️ Key rotation from 2026-08-03 STILL OUTSTANDING.)*
*Prior entry, 2026-08-04 (Mac session, FRONT-END OVERHAUL — see [OVERHAUL.md](OVERHAUL.md) for the full plan and per-step status. Shipped in ONE OTA update: wider color palette + 8 distinct stage hues; calendar week pager with real dates (Mon–Sat, Sundays dropped) and tappable month-grid job chips; job money section renamed "Invoices, Estimates, & Payments" with the amount/date overlap fixed and a looping stock-ticker stat header; payroll now knows it is submitted the Wednesday and paid the Friday after a period closes; per-job property artwork on pipeline cards. ⚠️ **Artwork is half-live**: migration 19 applied + `property-art` edge function deployed, but real Street View→Gemini cartoons need a `GOOGLE_API_KEY` secret Devon has not set yet — until then cards show a drawn placeholder house. ⚠️ Key rotation from 2026-08-03 STILL OUTSTANDING.)*
*Prior entry, 2026-08-04 (Mac session, pre-overhaul: workstation audit + gap closure — all 18 migrations verified applied against the live DB, build 27 confirmed live on TestFlight, `.claude/launch.json` path fixed, Vercel + EAS CLIs installed/linked/logged in, git push auth verified, secrets folder relocated to `~/Desktop/DC Solar LLC/secrets/`. See the new "Mac workstation" section. ⚠️ Key rotation from the 2026-08-03 note is still OUTSTANDING — Devon is doing it after today's changes.)*
*Prior entry, 2026-08-03 (Mac session: email scanner now LOGS transactions into finance_entries — DEPLOYED (notify v7 via Management API) and backfilled (13 entries since 7/24: 1 deposit $4,715 + 12 expenses $720.01, none job-matched — Chase alerts don't name jobs). Expense rows got a job picker in the edit card (assign to DC-26### or Company), shipped as an OTA update (commit 12db267) — this doubles as the pending OTA-adoption test on build 27: Devon should confirm it lands (close + reopen the app twice). ⚠️ Devon pasted the service-role key, sb_secret key, and a Supabase PAT into the session chat — rotate all three when convenient.)*
*New Claude Code session? Read this file and [PLAN.md](PLAN.md) first — they replace all prior context.*

## START HERE (state as of 2026-09-12, evening)

The dated paragraphs above are the change log. This section is the current state; if they disagree, this wins.

**Where the code is**

| Machine | Repo path | Notes |
|---|---|---|
| Devon's Windows PC | `C:\Durbin Enterprises\dev\DC-Solar-IOS-App` | Moved OUT of OneDrive 2026-09-12. OneDrive Files-On-Demand tags synced files as cloud reparse points, Node reports them as symlinks, Metro drops them and `expo export` dies; a `node_modules` junction did not survive either (OneDrive restores its cloud copy). Never put the repo or `node_modules` back under `C:\Users\devon\OneDrive`. Secrets: `C:\Durbin Enterprises\config\secrets\` (PAT, test-account passwords, README). |
| Devon's Mac | `~/Desktop/DC Solar LLC/DC-Solar-IOS-App` | see "Mac workstation" below |
| Carson's PC | `C:\Users\carso\Documents\DC-Solar-IOS-App-main\DC-Solar-IOS-App-main` | see "Carson's Windows workstation" below |

Two developers push to `main` (Devon's sessions and Carson's). **Always `git pull --ff-only` before anything.**

**How to ship**

- JS/TS change → `git push` (Vercel builds app.dcsolarkc.com from `main`) AND `npx eas-cli update --channel production --environment production --non-interactive --message "…"` from `app/`. Web never reaches the phones by itself.
- Native change (new native dep, app.json, entitlements) → bump `runtimeVersion` in `app.json`, `npx eas-cli build --platform ios --profile production --auto-submit --non-interactive`. **Current runtime is `4` (build 31, 2026-09-12).** Build-30 phones stay on the last runtime-3 update until they install build 31 from TestFlight.
- Before either: `npx tsc --noEmit` and `npx expo export --platform web` from `app/`, both clean.
- After a dependency change: `npx -y npm@10 install --package-lock-only` (EAS builders use npm 10).
- Migrations: write the file to `supabase/migrations/`, apply with `scripts\db\query.ps1 -TokenFile <PAT file> -File <migration>` (Windows: the `-TokenFile` is required; the script's default path is the Mac layout), prove RLS by impersonation in a rolled-back transaction (CLAUDE.md shows how).
- Edge functions: `POST /v1/projects/kjamxfezsathrsbztiln/functions/deploy?slug=<name>` multipart (`metadata` JSON with `verify_jwt` + `file`). The Management API `/secrets` endpoint returns hashes, not values.

**Shape of the app (since the 2026-09-12 overhaul)**

- Tabs: Home · CRM · Pipeline · Operations · Menu. Home = five colour-edged hub tiles (`lib/hub.ts`: CRM / Pipeline / Operations / Human Resources / Systems Management, `hubColors` in `constants/theme.ts`). Every role sees the same layout; admin-only doors are drawn locked and `lib/adminGate.ts` explains "contact your administrator" on tap and on deep links (17 admin screens mount `useAdminOnlyScreen()`). RLS remains the only real barrier.
- Palette: clean white base (`surfaceAlt #F6F7FA`, `surface` white) with vivid hub colours; `cream`/`sun` survive only for the login art and the action button.
- CRM ↔ Pipeline are cross-linked (job → customer, card → customer, live stage control on records, Jobs lens in the web workspace). Email is a full Gmail client on `gmail.modify` (`gmail-inbox` v20). Phone → Contacts is open to crew; texts/calls/keypad are admin. Hours logs many employees in one save (`LogHoursSheet`). Clock-in offers today's jobs + "Company office". Financials has Expenses and Receivables ledgers. Dropbox: one folder per job, every photo mirrored, folders follow renames. Contacts import from the iPhone (build 31+).

**Void rows:** `finance_entries.status = 'void'` is now excluded by every reader (merge cb76841); the one voided row keeps its real amount for the audit trail. **Payroll runs:** unique key is (company, period_end, receipt_id) with `kind` regular/off_cycle, `payroll_through` is the accrual cut-off, and the Hours tab records off-cycle runs (merge 9852a19).

**Receipts → Dropbox (2026-09-12):** every employee receipt photo is mirrored to `/DC Solar/Receipts/<YYYY-MM>/<date> <first name> <category> [<job>] <amount>.jpg` by a trigger on `receipts` + the same `dropbox-sync` function/cron as job photos (migration `2026-09-12_dropbox_receipts.sql`; backfill done for the existing receipts). Append-only: approve/reject/delete in the app never touch Dropbox.

**Books (2026-09-12):** ledger reconciled to the Chase export through 09/11 (proof + backups in that session's scratchpad, rows stamped `extracted.reconciled`); cash anchored to the 09/11 statement; `payroll_runs` complete through the 09/04 run (+ Ben's off-cycle 09/14) — one row per Gusto receipt, `kind` regular/off_cycle, and `company_settings.payroll_through` (advanced by regular runs only) is the single "paid through" for the Financials accrual and the cash position; the Financials chart (Revenue/Profit/Value) rebuilds from rows and follows realtime. Next reconciliation: export Chase again, match new lines the same way, re-anchor.

**Open for Devon (nothing else is blocked on code)**

1. App Store Connect → Pricing and Availability → untick "Make available on Mac with Apple silicon" (silences Apple's ITMS-90863 warning about `Testing.framework`; it is an Xcode 16 toolchain artefact).
2. Rotate `TWILIO_WEBHOOK_SECRET` when convenient (it was echoed into a session transcript on 2026-09-11; the Twilio signature gate still protects the endpoints): new value → edge-function secret, then the `?k=` on the Messaging Service inbound webhook, the number's voice webhook and the TwiML App URL.
3. Cloudflare Turnstile keys on the website (`dcsolarkc` repo/Vercel) — the quote form runs on honeypot + rate limit until then.
4. Carson's website test lead "Carson / Testing" (2026-09-07) is still an open lead + task; delete or keep.
5. Accounting: six 09/04 pay-stub PDFs + the service-role key file (for uploading them), vehicles/tools/loans in Financials → Company assets, and the classification questions in the 2026-09-12 reconciliation report (scratchpad) — see the late-night 2026-09-12 entry.
6. Judge the Receivables "Outstanding" tile (Σ max(invoiced − paid, 0) over Pending Payment jobs) and the clock-in default (first scheduled job, else Company office).

**Never verified on a device or in a signed-in browser** (no iPhone here; browser sign-in needs Devon to type a test password — `C:\Durbin Enterprises\config\secrets\test-accounts.txt` has `test-crew` (viewer) and `test-operator` (admin)): the whole overhaul as crew (locked tiles + alert), Gmail compose autosave, the CRM Jobs lens, ClockCard picker/switcher, contacts import, Receivables edit/delete, Phone Back/Home bar, a real Dropbox folder MOVE on renumber. First person to sign in on build 31 is the real test; `client_diagnostics` records JS crashes and notification/voice events per device.

**Small follow-ups nobody has claimed**

- `docs/CONTACTS.md` still says editing is contact-only (customers/crew editing was added later the same day).
- `messages/thread.tsx` and `crm/[id].tsx` still hint "update the contact in Messages settings".
- `shadows.hero` in `theme.ts` is unused since the Home header lost its band.
- Outbound email attachments, HTML email rendering, swipe actions on email rows.
- Dropbox: folder rename is DB-proven but no real move has happened yet; watch `dropbox_job_folders.last_error` after the first renumber.
- `.claude/launch.json` is machine-specific (Devon's now points at `scripts/dev-web.cmd` / `dev-native.cmd`); expect it to flip between machines.


## What this is

Employee field-ops app for DC Solar LLC (solar installation, Kansas City). One Expo codebase (`app/`) ships iOS (TestFlight) + web. Backend is the **same Supabase project the dcsolarkc.com website/ops console uses** — one database for everything. Owner: Devon Durbin (devonsd311@gmail.com), not a professional developer, mostly on Windows.

## Repo layout

- `app/` — the Expo app (SDK 57, TypeScript, expo-router, src/ layout). All app work happens here.
- `OVERHAUL.md` — the 2026-08-04 front-end overhaul: what changed, what's still blocked, and the two architecture corrections behind the artwork feature (Claude can't generate images; the generator runs as an edge function, not on the Windows PC).
- `supabase/migrations/` — dated SQL files, ALL APPLIED through `2026-09-12_payroll_runs_receipt_key.sql` (sessions apply them via the Management API; the file is the record). `supabase/functions/` — the Deno edge functions, deployed the same way.
- `PLAN.md` — original build plan + phases; still the roadmap.
- `HANDOFF.md` — this file. Keep it updated at the end of every session.
- NOT in git: `app/.env` (recreate — see below), `app/node_modules`, `data/` (local business-data exports; the DB is the source of truth), `website/` (separate repo: github.com/durby48/dcsolarkc).
- `docs/` — one setup/design doc per integration (TWILIO, GMAIL, CRM_EMAIL, DROPBOX, CONTACTS, NOTIFICATIONS, LEAD_INTAKE, AUTOMATION, CARD_FORGE, MARKETING, PDF_RENDERING, SOCIAL_LOGIN). Read the matching one before touching an integration.

## Accounts & IDs

- Supabase: https://kjamxfezsathrsbztiln.supabase.co — publishable key `sb_publishable_rETJcVvcbKk79wOFSNIlTg_CEFCfbdF` (client-safe). The **secret key** is in Supabase dashboard → Settings → API keys — needed only for admin scripts, never in the app or git.
- Expo/EAS: account `durby`, org `dc-solar`, project `dc-solar-kc` (id c1bf33f2-33a6-4730-9fb3-4b98405c2c82). `npx eas-cli login` once per machine.
- Apple: ASC App ID 6794484032, bundle `com.dcsolarkc.fieldapp`, Apple ID devonsd311@gmail.com. TestFlight: https://appstoreconnect.apple.com/apps/6794484032/testflight/ios
- Builds shipped: #2 (first TestFlight), #3 (Pipeline/PM/More), #4 (stages/totals), #5 (picker fix, stage refresh, finance editing, skyline icon), #14 (session 2: pipeline Contracted/Invoiced buckets + per-completed-job Avg Profit, Pending Install stage, in-app PDF view + share sheet, home-screen widget; #6–#13 were burned by failed attempts — autoIncrement bumps on every try), #18 (session 3, 2026-07-27: same code as #17's commit — verse of the day, job reminders, push-token registration, aps-environment entitlement. #15/#16 burned: provisioning profile lacked Push Notifications capability until Devon enabled it on the App ID; #17 burned: broken Node binary on the EAS builder, infra flake, retry fixed it), #19 (session 3: Financials tab — admin expense dashboard + ledger + add/edit/delete), #20 (session 3: pipeline stage-filter chips, estimate Supplement checkbox, broadened money-word pushes), #21 (session 3: completed_on dates, Active filter, contracts-bucket upsert fix — migration 10), #22 (session 3: photo-upload auto-retry with fresh-path retries, fresh PDFs share under their real document names via `shareLocalPdf`), #23 (session 3: Financials pipeline-mirror card + /ledger drill-downs for estimates/invoices/contracted/paid with PDF actions and period/job-status filters), #24 (session 3: editable contract values on /ledger/contracted via a per-job "Contract value" invoice entry, with a per-job Generate-contract-PDF toggle producing `<job>-Contract.pdf` — off for historical jobs with uploaded signed contracts), #25 (session 3: Materials section + PDF extraction, demo mode removed, web back arrows), #26 (session 3: **first OTA-capable build** — expo-updates enabled; crew must run ≥26 to receive `eas update` pushes). First OTA update published 2026-07-27: **Calendar tab** (Today renamed; This-week card lists Tomorrow→Saturday from job_schedule_dates, explicit "No work for tomorrow", blank later empty days; clock card unchanged). Further OTA updates same day: admin photo delete (migration 12), customer insurance PDFs (migration 13). ⚠️ Devon's build 26 did NOT pick up OTA updates despite correct channel/runtime/env config (cause unconfirmed — possibly device download flake or silent rollback); **build 27** shipped with everything embedded. **CONFIRMED LIVE on TestFlight 2026-08-04 (Devon sees build 27 on his device), so build 26 is superseded and its OTA failure is moot.** OTA adoption on 27 itself is still unproven — the next `eas update` doubles as that test; if a JS-only change doesn't land after two app relaunches, fall back to a full build rather than debugging blind.
- Apple Team ID: E4B2Y6BWCH (in app.json ios.appleTeamId — needed by @bacons/apple-targets).
- EAS env vars (production): EXPO_PUBLIC_SUPABASE_URL/KEY now live on EAS servers — needed because the repo-root .gitignore excludes `.env` from EAS uploads (day-one builds predated the git repo, so this only bit now).
- **EAS build gotchas (learned the hard way on 2026-07-24):**
  - EAS builders run npm 10; local Mac has npm 11. After adding/updating deps, regenerate the lock with `npx -y npm@10 install --package-lock-only` and verify `npx -y npm@10 ci --dry-run`, or the build dies at Install dependencies with "lock file out of sync".
  - eas-cli's App Groups capability auto-sync hits an Apple API bug ("request entity is not a valid request document object"). App Groups were enabled manually in the Apple Developer portal (group.com.dcsolarkc.fieldapp on both com.dcsolarkc.fieldapp and .widget) — done, shouldn't recur.
  - Creating a provisioning profile for a NEW target requires a real Apple ID login (interactive) once; after that, builds are fully `--non-interactive` again.
- Twilio: **LIVE as of 2026-09-06.** Account owner devon@dcsolarkc.com, number (816) 744-6473 = `+18167446473`, Messaging Service `MGa716f2247019db31a3bf2405d38d512d`, A2P 10DLC campaign Verified (EIN 93-3073873). Secrets are edge-function secrets only (`TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_FROM_NUMBER`, `TWILIO_MESSAGING_SERVICE_SID`, `TWILIO_WEBHOOK_SECRET`, `TWILIO_PUBLIC_BASE`) — never in a table, never in git. Setup reference: `docs/TWILIO_SETUP.md`.
- **⚠️ Metro minifier must keep function names** (`app/metro.config.js`, added 2026-07-27): terser's name-mangling white-screened the production WEB build with zero console errors — React Navigation reads a screen component's function name (lowercase first letter = render function) and mangled names broke rendering. `keep_fnames`/`keep_classnames` fixes it; dev builds never show the problem, so test web changes against a real `expo export`. Applies to iOS bundles too (slightly larger, harmless).
- **Hours tab (2026-08-03, OTA):** 5th tab, admin-only (`(tabs)/hours.tsx`, `lib/payroll.ts`). Per-employee payroll view from employee_hours + completed time_entries: YTD hours, period hours, pay (hours × stamped rate, roster fallback; `*` marks missing-rate understatement), expandable per-job rows with a **Paid before | This period** split for carry-over jobs. Period pager: "Before Jul 18" bucket (paid via old spreadsheet), catch-up period 7/18–8/3, then clean biweekly periods from 8/4 — all computed in `lib/payroll.ts` constants, no DB table. **Pay cycle (added 2026-08-04):** a period is SUBMITTED the Wednesday after it closes and PAID the Friday after — the period ending Mon 8/3 is submitted Wed 8/5 and paid Fri 8/7. `PayrollPeriod.submitOn`/`payOn` + `payrollState()` drive a status chip (current / closed-not-submitted / submitted / paid) and a Submit|Payday row, so a closed-but-unpaid period no longer misreports as "Paid". First payroll run off this tab: 172.9 h / $5,692.33 (8/3). NOTE: DC-26011's removal hours predate app tracking and were never logged against the job, so its "Paid before" shows "—"; Devon can backfill via the job screen's admin hours if wanted.
- **Web app (reworked 2026-08-03):** LIVE at **https://app.dcsolarkc.com** (SSL issued; DNS CNAME `app` → `name.vercel-dns.com` added by Devon in Squarespace) and https://dc-solar-app.vercel.app. Vercel project `dc-solar-app` now lives under the Pro team **devon-durbins-projects** (login devonsd311@gmail.com) and is **connected to the GitHub repo — every push to `main` auto-builds and deploys the web app.** Build settings (set 2026-08-03 via dashboard): Root Directory `app`, Build Command `npx expo export --platform web`, Output Directory `dist`, framework Other; env vars EXPO_PUBLIC_SUPABASE_URL/KEY on Production+Preview. Icon fonts verified serving (the old CLI-deploy `.vercelignore` hack is NOT needed for git builds). The manual `vercel deploy` workflow in Windows-setup step 7 is OBSOLETE — just push to main. Same Supabase/auth/RLS as iOS; native-only features degrade gracefully on web.

## Devon's Windows workstation (verified 2026-09-12)

Repo at `C:Durbin EnterprisesdevDC-Solar-IOS-App` (see START HERE for why it left OneDrive). Everything needed to ship is wired:

| Capability | State | How it's wired |
|---|---|---|
| Typecheck / web export | ✅ both pass | Node v24 (winget: pass `--source winget`), npm 12; `npm ci` in `app/` |
| Git push | ✅ | Git Credential Manager; `core.autocrlf=false` on the clone |
| EAS (OTA + builds) | ✅ logged in as `durby` | `npx eas-cli whoami`; builds 30 and 31 and every OTA since 2026-09-11 went from here |
| Supabase admin (DDL, deploys, RLS probes) | ✅ | PAT in `C:Durbin Enterprisesconfigsecretssupabase-access-token.txt`; `scriptsdbquery.ps1 -TokenFile <that file>` (the `-TokenFile` is required on this PC) |
| Service-role key | ❌ not on this PC | only needed for the Plaid/reconcile scripts |
| Test logins | ✅ | `test-crew` (viewer) / `test-operator` (admin) passwords in `secrets	est-accounts.txt` — Claude will not type them; Devon signs in on the Browser pane when a crew-eye check is wanted |
| Browser preview | ✅ | `.claude/launch.json` → `scripts/dev-web.cmd` (port 8082) / `dev-native.cmd` (8081); PowerShell's execution policy blocks `npx.ps1`, hence the .cmd wrappers |

## Mac workstation (verified end-to-end 2026-08-04)

Repo lives at `~/Desktop/DC Solar LLC/DC-Solar-IOS-App`. Everything needed to ship from this Mac is in place — verified, not assumed:

| Capability | State | How it's wired |
|---|---|---|
| Typecheck | ✅ `npx tsc --noEmit` exits 0 | node v24.12.0 via nvm, npm 11.6.2, `app/node_modules` installed |
| Web export | ✅ `npx expo export --platform web` succeeds | writes `app/dist` |
| Git push | ✅ authenticated (verified by dry-run push) | osxkeychain helper from Xcode's gitconfig; identity Devon Durbin / devonsd311@gmail.com |
| Vercel | ✅ CLI 58.5.1 logged in, `app/.vercel` linked to `dc-solar-app` | scope `devon-durbins-projects`. Deploys still happen automatically on push to `main` — CLI is for logs/env only |
| EAS | ✅ eas-cli 21.5.0, logged in as `durby` | OTA `eas update` and full builds can both be run from here |
| Supabase admin | ✅ Management API verified (listed tables, policies, functions) | PAT + service-role keys in `~/Desktop/DC Solar LLC/secrets/` |
| Xcode | ✅ 26.4 | local iOS builds possible if ever needed |

- **Secrets folder (NOT in git, never commit):** `~/Desktop/DC Solar LLC/secrets/` — `supabase-access-token.txt` (PAT, Management API), `supabase-service-role-keys.txt` (RLS-bypassing, both projects), `gmail-sa-*.json`, `ops-token-secrets.txt`, `resend-api-keys.txt`, `claude-code-token.txt`. See its `README.md`.
- **`gh` CLI is NOT installed** — Homebrew refused (`/opt/homebrew` is owned by another user). Not required for anything in this workflow (plain `git push` works). If wanted: `sudo chown -R $(whoami) /opt/homebrew && brew install gh`.
- `.claude/launch.json` fixed 2026-08-04 (had a stale `/Users/areesasteele/...` path from another machine); now has `expo` (port 8081) and `expo-web` (8082) configs.

## Carson's Windows workstation (set up 2026-08-18)

Second machine: `C:\Users\carso\Documents\DC-Solar-IOS-App-main\DC-Solar-IOS-App-main` (Carson Keating's PC; Devon works with him here). The folder began as a GitHub zip download and was turned into a real clone of `durby48/DC-Solar-IOS-App` (`carsonkeat` has write access; local `main` tracks `origin/main`, tree matched exactly). Every DC Solar account is signed in on **Microsoft Edge** here, but the Claude browser extension only reaches Google Chrome (Carson's accounts) — so all DC Solar work runs off tokens, not the browser.

| Capability | State | How it's wired |
|---|---|---|
| Typecheck | ✅ `npx tsc --noEmit` exits 0 | node v24.12.0, npm 11.7.0 (system install), `app/node_modules` installed |
| Web export | ✅ `npx expo export --platform web` succeeds | writes `app/dist` |
| Git push | ✅ `gh` logged in as `carsonkeat` (WRITE on the repo) | identity Carson Keating / carsonke4ting@gmail.com; `core.autocrlf=false` so the LF files stay LF |
| Vercel | ➖ not needed | web deploys on push to `main` from Devon's team; the local CLI (54.x) is Carson's account, unlinked |
| EAS | ✅ verified (`eas whoami` → durby via EXPO_TOKEN; `project:info` → c1bf33f2; build + update lists readable) | Personal access token `carson-windows` on the `durby` account (no expiry) in `secrets/expo-token.txt`. **Every eas command on this PC must be run with `$env:EXPO_TOKEN` set from that file** — the local `eas login` is Carson's `rsntrippy`, which EAS rejects for this project. Never run `eas login` here. Project resolves as `@durby/dc-solar-kc`. |
| Supabase admin (DDL, deploys, RLS probes) | ✅ Management API verified (SQL query + rolled-back RLS probe as a viewer: 0 finance rows) | PAT `carson-windows` (Devon's account, expires **2027-02-18**) in `secrets/supabase-access-token.txt`. Local `supabase` CLI 2.67 is Carson's account — pass `SUPABASE_ACCESS_TOKEN` if the CLI is ever needed. |
| Supabase service-role (supabase-js admin reads/writes, reconcile + Plaid scripts) | ⏳ **needs `supabase-service-role-keys.txt`** | Devon declined for now (2026-08-18); copy from DC Solar KC → Project Settings → API keys when wanted |
| Secrets folder | ✅ created | `C:\Users\carso\Desktop\DC Solar LLC\secrets\` — same `~/Desktop/DC Solar LLC/secrets` layout the Mac scripts resolve via `homedir()`; its `README.md` lists every file + format |
| Browser | ✅ Claude extension paired to Devon's **Chrome profile "devon"** (Supabase DC Solar KC, Expo `durby`, Vercel `devon-durbins-projects`, GitHub `durby48` all signed in) | Edge is NOT reachable; pick "Browser 1"/the Devon profile when two extensions show up |
| `.claude/launch.json` | ✅ rewritten for Windows (`cmd.exe`, this path) | machine-specific by nature — the Mac copy had the Mac path; expect this file to flip between machines |
| ffmpeg | ❌ not installed | only needed to re-cut the login WebP |

npm gotcha: npm 11 rewrites `app/package-lock.json` on `npm install` (drops one optional peer entry). Restore it with `git checkout -- app/package-lock.json` (or use `npm ci`) — the committed lock is the npm-10 one EAS builders need.

## Built 2026-08-18 on Carson's PC — SHIPPED same day on Devon's "push the updates" (migrations 29–30 applied, pushed to main → web auto-deploy, OTA to production)

Built local-first: each migration was proven inside a rolled-back transaction (`scripts/db/query.ps1 -Rollback`) with viewer/admin/outsider impersonation, every UI piece was rendered in the local web build via a throwaway preview route (deleted), typecheck + web export passed — and only then applied/committed/pushed/`eas update`d. After applying, the live tables were re-checked with a real sign-in as the test viewer (EOM readable, insert denied, marketing_secrets 0, finance 0). This OTA also carries the 10 commits from 2026-08-07/09 that had never reached the phones (Sales tab, capital, Net-includes-labor, cash position, Plaid scripts) — the first thing to check on a phone is that More now shows an `update <id>` line.

**Build 28 (2026-08-18 evening):** Devon reported the OTA (group `d05c389b`, iOS update `01a01708…`) not appearing on his phone. Server side was verified correct (channel production → branch production, runtime 1.0.0; the manifest endpoint served the new update to a build-27-shaped client), so per the standing rule — "if a JS-only change doesn't land after two relaunches, fall back to a full build rather than debugging blind" — build 28 was made from commit `ee0c3db` (`eas build --platform ios --profile production --auto-submit`, EAS build `b0b125b7`, built in 5 min). Version stays 1.0.0 so runtime is unchanged and future OTAs still target it. Submission `08e13f41` sat **70 min "in queue"** because of an Expo incident that day ("iOS submissions hanging on App Store Connect build uploads — Apple's API reporting prolonged pending/processing"), then uploaded at 7:52 PM. **Still open:** whether build 27 ever applied any OTA. Once on build 28, the More footer makes that answerable: publish, relaunch twice, watch the id change. If build 28 also never shows an `update …` line, treat expo-updates as broken in the binary and investigate the app config before the next OTA-only ship.

- **Build/OTA indicator** — `components/BuildInfo.tsx` at the bottom of More: `DC Solar KC 1.0.0 · runtime 1.0.0 · production` + `update <8-char id> · <time>` (or "embedded bundle" / "web build" / "development build"); on a real build, tapping it runs check → fetch → reload. This is how the open "does OTA land on build 27" question finally gets answered: publish, relaunch, watch the id change.
- **Employee of the Month** — Garrett Nimsgern every month until further notice, a different photo each month, encoded as DATA: `supabase/migrations/2026-08-18_employee_of_month.sql` (table `employee_of_month`, PK (company, month), month must be the 1st, `employee_name` filled by trigger from the roster, seeds the current month with no photo). RLS: SELECT `is_company_member`, INSERT/UPDATE/DELETE `is_company_admin` — proven: viewer reads 1, viewer insert 42501, admin insert OK, outsider 0, mid-month date 23514, viewer still 0 finance rows. Photos go in the existing private `job-photos` bucket under `eom/` (no new storage policies — bucket already member-read/admin-delete). `lib/eom.ts` (current month else latest past month, always labelled with the current month; signed-out → mock initials card; missing table → renders nothing), `components/EmployeeOfMonth.tsx` mounted on Today for every role, admin manager at More → Employee of the Month (`more/employee-of-month.tsx`). **Devon must supply the Garrett photos.** Card carries name/email/photo/caption only — never money.
- **Sales → "Leads | Marketing" segment** — `components/MarketingPanel.tsx` + `lib/marketing.ts`: reputation header, GBP/Facebook/Instagram/Yelp metric cards over 7/28/90-day chips, recent reviews with admin-only Reply, admin-only Connect buttons that explain the OAuth prerequisite (no OAuth flow yet). Falls back to clearly-labelled MOCK data when signed out / tables missing / zero connections. `supabase/migrations/2026-08-18_marketing.sql`: `marketing_connections`, `marketing_metrics`, `marketing_reviews` (member read, admin write) + `marketing_secrets` (RLS on, ZERO policies — service role only; proven invisible even to Devon). `supabase/functions/marketing-sync/index.ts` is a NOT-deployed skeleton written from the GBP Performance / My Business reviews / Meta Graph contracts — never executed. Owner steps in `docs/MARKETING_SETUP.md` (GBP API access request has a waiting period — start early).
- **Website SEO** (separate repo, local copy `C:\Users\carso\Documents\dcsolarkc-main\dcsolarkc-main`, uncommitted): OG image route, canonical fix, `SolarInstallationBusiness` + 2 Service + WebSite + FAQPage JSON-LD graph, 12-city areaServed + honest "Areas we serve" section, robots noindex on /login + /api, aria-hidden on decorative icons, heading fix. `SEO-AUDIT.md` at that repo root has the top-10, the verified NAP (816-274-2415 / devon@dcsolarkc.com / Lee's Summit), the owner TODOs (social URLs, GBP review short-link, hours/geo) and the baseline-capture checklist (Search Console, PageSpeed, GBP Insights).
- **Test tooling** — `scripts/db/query.ps1` (SQL via Management API; `-Rollback -StripTransaction` to dry-run a migration file), `supabase/recovery/test-accounts.md` — **test viewer `test-crew@dcsolarkc.com` CREATED 2026-08-18** (employees role viewer, password in Carson's `secrets/test-accounts.txt`); verified by real sign-in + REST: finance 0 / hours 0 / time_entries 0 / jobs 30 / customers 19 / own employees row only. Use it to look at the app as crew; delete with the SQL in that doc.
- **DC-26026 fully separated from jobs on the phone (2026-08-18):** Devon saw the company container listed as a job at "−5790.9%". Cause: a $434.85 Chase deposit (8/10) that the email scanner filed to the container because it matched no job, divided by $25.6k of overhead. `main` had already pulled it out of the Financials per-job P&L (64c3148, never OTA'd), but the PHONE pipeline card still rendered a normal page 2 for it. Now `pipeline.tsx`: `PipelineCard` special-cases `isCompanyJob` — page 2 shows **Overhead + Hours logged**, a "company costs" note, and a ⚠ line whenever any payment is filed against it (a payment there is always a misfiling); no Modules/forecast/Est/Inv/Paid/Profit; page 1 reads "Company overhead — not a project". The **All** filter now lists projects only (count too) — the container lives under its own **Company** chip. Financials' "Company overhead" row gained the same ⚠ "payments filed under Company — assign to a job" line. **Devon still needs to reassign that $434.85 deposit** to its real job from the payment row (I did not guess). Recommendation, not done (edge function = deploy): make `notify` file unmatched *payments* with `job_id null` (a "needs a job" queue) instead of onto the container.
- **Not done, by decision/blocked:** OTA of the 10 pending commits (Devon said local only); Plaid sandbox (no `plaid.txt` yet); QuickBooks (pending decision on system of record); own-pay-rate visibility for crew (pending decision).

## Windows setup (first time)

1. Install Node LTS (nodejs.org) and Git; clone this repo.
2. `cd app && npm install`
3. Create `app/.env`:
   ```
   EXPO_PUBLIC_SUPABASE_URL=https://kjamxfezsathrsbztiln.supabase.co
   EXPO_PUBLIC_SUPABASE_KEY=sb_publishable_rETJcVvcbKk79wOFSNIlTg_CEFCfbdF
   ```
4. Dev: `npx expo start` → scan QR with Expo Go on iPhone (same Wi-Fi), or press `w` for web.
5. **Ship (NEW WORKFLOW since build 26 — OTA updates):** JS/TS-only changes ship with `npx eas-cli update --channel production --environment production --non-interactive --message "<what changed>"` (the `--environment` flag is REQUIRED in non-interactive mode) — reaches installed apps (build ≥26) on next launch, costs NO build quota. Full native builds (`npx eas-cli build --platform ios --profile production --auto-submit --non-interactive`) are needed ONLY when native things change: new native dependency, app.json/plugins/entitlements, expo-updates config, widget code, SDK upgrade. Version bump (1.0.0 → 1.0.x) changes the runtimeVersion → needs a build. Quota note (2026-07-27): Devon nearly exhausted the free plan's monthly builds; OTA exists precisely to stop that. Do NOT create extra Expo accounts to dodge quota.
6. Verify before shipping: `npx tsc --noEmit` and `npx expo export --platform web` must both pass.
7. Ship web: nothing to do — Vercel builds app.dcsolarkc.com from every push to `main` (root `app`, `npx expo export --platform web`, output `dist`). The old manual `vercel deploy` steps are obsolete.

## People / logins

All 6 employees have Supabase auth logins; shared temp password `DCSolarKC2026` with forced personal-password change on first app sign-in (`must_change_password` user_metadata flag → /set-password screen).

| Person | Email (app identity) | Role | Rate |
|---|---|---|---|
| Devon Durbin | devonsd311@gmail.com | owner (admin) | $35 |
| Isaiah Nettleton | inettleton18@gmail.com | operator (admin) | $33 |
| Clark Lynch | clarklynch30@gmail.com | operator (admin) | — (not set; added 2026-08-03, temp pw DCSolarKC26 + forced change) |
| Ben Nettleton | bnettleton403@gmail.com | viewer | $35 |
| Simon Nettleton | snettleton2005@gmail.com | viewer | $30 |
| Garrett Nimsgern | gnimsgern.2022@gmail.com | viewer | $30 |
| Tyler Sisson | tylersisson99@yahoo.com | viewer | $30 |

⚠️ Isaiah also has isaiah.nettleton@dcsolarkc.com (website console only). Its `employees` row was DELETED on purpose so it cannot record app data — his hours land only under the gmail identity. Do not re-add it.

## Key architecture decisions (do not accidentally undo)

- **RLS is the security model.** Admins = employees.role in (owner, operator) via `is_company_admin()`; everyone else sees only their own rows. Finance data, job editing, scheduling, paystub uploads: admin-only at the DB level.
- **Stages vs status:** jobs have a `stage` column (7 pipeline stages, `app/src/lib/stages.ts`) — the app's vocabulary. The legacy `status` (active/completed/on_hold) is kept in sync via `statusForStage()` because the dcsolarkc.com ops console still uses it.
- **Job numbering** (`DC-26###`) is computed from the live jobs table at save time (max+1) — shared with the ops console, no separate counter. Next: DC-26019.
- **finance_entries** got admin UPDATE/DELETE only in migration 7; older code paths assume insert-only — the app's edit UI shows a friendly migration message if #7 isn't applied.
- **Receipts** are a review queue: crew insert → admin approve → the approval INSERTS a finance_entries expense (tagged in `extracted`). Don't let crew write finance_entries directly.
- **Location**: foreground-only GPS at clock in/out (no background tracking yet — that's a future phase with a consent policy).
- **Pipeline header buckets (session 2):** "Contracted" = invoice-entry totals of jobs in Pending Removal/Reinstall/Install/Permit; "Invoiced" = Pending Payment jobs only; "Avg Profit" = mean of per-job (paid − expenses − labor)/paid over Complete jobs with payments. All in `lib/pipeline.ts::fetchCompanyTotals` (needs the jobs list for stages).
- **Home-screen widget (session 2):** WidgetKit target in `app/targets/widget/` via @bacons/apple-targets; app pushes today's job + clock state through App Group `group.com.dcsolarkc.fieldapp` (`lib/widget.ts`, called from the Today screen). Widget shows a live timer while clocked in. Data refreshes only when the app runs — no background fetch.
- **PDF view/share (session 2):** `lib/pdf.ts` — in-app browser sheet for viewing, expo-file-system download + share sheet (iMessage etc.) for sharing. Used by JobDocuments, JobInvoices, paystubs.
- Historic data imported from Devon's P&L spreadsheet is tagged `pnl-import-2026-07-24` (finance_entries.extracted / employee_hours description).
- **Pipeline stage filter (session 3):** chip row under the totals header — All + Active (= everything except Complete) + the 8 stages, each with a live count; filters the job list client-side. State is per-visit (resets to All).
- **jobs.completed_on (migration 10):** date stamped automatically when a job's stage is set to Complete (editable in the job editor; cleared when the stage leaves Complete). Complete cards on the Pipeline show "Completed <date>" instead of the next-date line. Column-fallback machinery in `lib/jobs.ts::payloadAttempts` now generates subsets over three optional column groups (stage / PM / completed_on).
- **contracts bucket fix (migration 10):** document PDF uploads use upsert — the bucket was missing UPDATE/DELETE policies, so re-generating an existing file failed with an RLS violation ("cloud copy failed to save"). Migration 10 adds admin UPDATE + DELETE.
- **Estimate "Supplement" checkbox (session 3):** in the document builder (estimates only) — adds a prefilled line item: name "Supplement", $136/panel, qty 1 (Devon adjusts qty per estimate), with the racking-&-railing warranty paragraph as the item description (prints under the item name on the PDF; constants at the top of `document-builder.tsx`).
- **Ledger drill-downs (session 3, build 23):** the Financials tab mirrors the Pipeline header numbers (same `fetchCompanyTotals`, same rows) in a second card whose tiles open `/ledger/[view]` (`app/src/app/ledger/[view].tsx`): estimates (every estimate + PDF view/share, grouped per job, "current estimate" footnote), invoices (every invoice + PDF, active/completed job filter + All/Month/Quarter/YTD period chips), contracted (jobs in contracted stages + contract values, tap → job), paid (payments grouped per job). All admin-only, all client-side — no new tables.
- **Crew assignments + calendar views (2026-07-31, migration 16, OTA):** `job_assignments` (job×email, roster-name fill trigger, member read/admin write). Job screen "Assigned crew" chips (admin toggle). Calendar: admin Week|Month toggle — Month is a navigable agenda via `fetchScheduleRange`; assigned crew first names show on every calendar entry. Crew hours/my-hours (migrations 14–15): own-row entries + admin manage-all, hours×date no clock times, roster rate via trigger.
- **Calendar week pager (2026-08-04, OTA):** the week list is now Monday→Saturday with a real date on every row ("Mon 8/3"), a ‹ › pager into past and future weeks, a "Back to this week" chip when off-current, and today highlighted. **Sundays are omitted entirely** from the week list (nobody works Sunday) — the month grid keeps its Sunday column so it still lines up like a normal calendar. Past weeks need their own `fetchScheduleRange` call because the default schedule fetch only reaches 7 days back. Month-grid job chips are now Pressables that route to `/job/[id]`, matching week-view behavior; tapping elsewhere in a day cell still just selects the day.
- **Job money section (2026-08-04, OTA):** `JobFinanceHeader` is a looping stock-ticker — the tile track is rendered twice end-to-end and translated by exactly one track width (measured via `onLayout`) so the loop is seamless; `useNativeDriver` on native, off on web; falls back to a static scroller when the OS reports reduce-motion. Tiles are colored from `accentCycle`. `JobInvoices` header renamed to "Invoices, Estimates, & Payments", and the row was restructured (`icon | title+date | right column of amount-over-actions`) because the amount used to collide with the recorded date on narrow phones. Payment amounts render green, expenses coral.
- **Materials (session 3, migration 11, build 25):** job Materials section is live (`components/JobMaterials.tsx`, `lib/materials.ts`, `job_materials` table — crew read, admin write, name × qty only, NO pricing). Admins add items manually, upload materials PDFs (doc_type 'materials', contracts bucket), and tap Extract → `supabase/functions/extract-materials` (verify-JWT ON + admin check; unpdf text extraction + qty heuristics) returns candidate items reviewed with checkboxes before saving. Extraction is heuristic — expect to uncheck junk rows on messy PDFs.
- **Edge functions deploy via the Management API** (no dashboard paste needed): `POST /v1/projects/<ref>/functions/deploy?slug=<name>` multipart with `metadata` (JSON: entrypoint_path/name/verify_jwt) + `file`. notify=verify_jwt FALSE (shared secret), extract-materials=verify_jwt TRUE. notify redeployed to v4 (broadened money words) 2026-07-27.
- **Demo mode removed from the sign-in screen** (session 3, build 25): crew must sign in; the mock-data fallbacks remain in the libs but there is no UI entry point. Web-only header back button lives in `_layout.tsx` (native-stack renders no arrow on web).
- **Direct SQL access (2026-07-27):** Devon issued a Supabase personal access token (stored OUTSIDE the repo — Windows: `C:\Durbin Enterprises\config\secrets\supabase-access-token.txt`; Mac: `~/Desktop/DC Solar LLC/secrets/supabase-access-token.txt`); sessions run migrations via the Management API `database/query` endpoint — still write each migration file to `supabase/migrations/` for the record.
- **Financials tab (session 3):** 4th tab between Pipeline and More (`(tabs)/financials.tsx`, data in `lib/financials.ts`). Admin-only (viewers/demo get a friendly placeholder). Overview tiles (Paid in / Expenses / Net / This month) + the full company expense ledger grouped by month with subtotals, each row editable/deletable via the existing `updateFinanceEntry`/`deleteFinanceEntry`. "+ Add expense" inserts a finance_entries row (type=expense, direction=out, status=recorded — same shape receipts approval writes) with optional job chip (job_id null = company overhead), so Pipeline profit math picks job-tied ones up automatically.

- **Property artwork (2026-08-04, migration 19, OTA):** every pipeline card gets a cartoon of its OWN property as a semi-opaque background. `job_artwork` table + private `property-art` bucket; edge function `property-art` (verify_jwt ON, re-checks admin server-side) does Street View metadata → Street View Static image → Gemini `gemini-2.5-flash-image` image-to-image cartoon → store → mark ready. Admin UI is the "Property artwork" card on the job screen (`components/JobArtwork.tsx`): Generate from address, or Use a photo (picks from the job's existing job_photos — that's the override for addresses Street View can't see). `lib/artwork.ts` signs all pipeline URLs in ONE batched request. Results cached; regeneration is opt-in (`force`) so browsing never re-bills. **BOTH keys are now set on the function (2026-08-04): `GOOGLE_API_KEY` (Maps/Street View) and `GEMINI_API_KEY` (AI Studio; note AI Studio issues `AQ.*` keys now, not `AIza*`). Google will NOT authorize one key for both Maps and generativelanguage.googleapis.com — the Cloud Console greys Gemini out of a Maps key's restriction list, hence two keys. Street View verified working; Gemini auth verified working and `gemini-2.5-flash-image` confirmed to exist. Devon loaded $10 of prepay credit 2026-08-04 and generation now WORKS end-to-end at the API level (verified: real Street View shot of 101 W 115th St → a correct cartoon of that ranch house). **Model is `gemini-3.1-flash-image` at 16:9** — chosen by side-by-side test: `gemini-2.5-flash-image` invented a two-storey house with a left garage for a single-storey brick ranch, i.e. it drew *a* house rather than *the* house. 3.1 preserved storey count, brick colour, roofline and garage side. ~4¢/image, cached forever after. Old note follows: `GOOGLE_API_KEY` IS SET on the function (2026-08-04, key also in `~/Desktop/DC Solar LLC/secrets/google-api-key.txt`). Street View Static API verified WORKING; Generative Language API (Gemini) is BLOCKED on that key — direct call returns 403 `API_KEY_SERVICE_BLOCKED`, meaning the key's API-restriction list omits generativelanguage.googleapis.com. Devon must enable the API in the Cloud Console Library AND add it to the key's API restrictions. Until then artwork generation reaches the Gemini step and fails with that message — until then every card falls back to `components/PropertyArt.tsx`'s drawn house, which is seeded from the job id so a job always draws the same one.** Note for future sessions: the Street View + Gemini calls have never executed — they are written from the API contracts, not tested.
- **No new native deps, deliberately:** `react-native-svg` and `expo-linear-gradient` are NOT installed, and adding either forces a full EAS build. Everything in the overhaul (property art, ticker animation) is built from RN primitives + `Animated` so it could ship OTA. If you ever want real gradients or SVG, plan it as its own native release.

- **Pipeline hero + company metrics (2026-08-05, OTA):** the finance totals card is GONE from the pipeline header (unchanged on the Financials tab — `fetchCompanyTotals` is still used there, do not delete it). In its place `components/PipelineHero.tsx` cycles three illustrated installer frames in a 1→0→2→0 loop, over a `components/Ticker.tsx` band of milestones from `lib/metrics.ts`: panels R&R'd, projects completed, total company hours, critter guard panels. Ticker logic was EXTRACTED from JobFinanceHeader — both now share `Ticker.tsx`; don't re-fork it.
- **Two-page job cards (2026-08-05):** each pipeline card is a paging horizontal ScrollView over ONE shared `PropertyArt` background. Page 1 = number/stage/customer+avatar/address/next date. Page 2 = modules, forecast hours, Est/Inv/Paid, profit %. Money only renders when `money` is non-null, which RLS already restricts to admins.
- **Module counts + job type (migration 20):** counts only ever existed inside job NAMES in six formats (incl. a typo'd "18 mobules"); `lib/jobs.ts::parseModuleCount` resolves 19 of 25 and only SEEDS `jobs.module_count`, which is then editable in the job editor. Deriving stats by re-parsing text live was rejected on purpose — a typo fix would otherwise move company statistics. `job_type` (R&R/Reinstall/Install/Critter Guard/Other) picks the forecast coefficient. `is_internal` keeps the company container out of customer metrics.
- **Critter guard is a FLAG, not a job type (2026-08-05, migration 21):** `jobs.has_critter_guard` — any job can carry it, because most critter guard is installed as part of an R&R and migration 20 could only record it as a standalone `job_type='Critter Guard'` job, leaving those panels invisible. The job editor shows a checkbox on every job type plus an optional panel count; **blank means the whole array**, and `lib/metrics.ts` resolves it as `critter_guard_panels ?? module_count`, so correcting a module count later keeps the critter total right with no re-editing. **Only COMPLETE jobs contribute** — a flagged job still in progress counts 0 (verified: DC-26002 Complete → 44, DC-26025 Pending Estimate → 0). The `Critter Guard` job_type is kept for the rare standalone job. Pipeline card page 2 shows a lime "Critter guard" chip when the flag is set.
- **DC-26026 is now "DC Solar Company" (2026-08-05):** renamed from "Company Home Base" and repurposed as THE company-level container — overhead expenses, bulk materials, shop photos, non-job hours. All 16 previously untagged overhead expenses ($16,633.74) and 1 untagged hours row were migrated onto it; `finance_entries.job_id` and `employee_hours.job_id` are now never null for dc-solar. The "Company" chip in both Financials expense pickers writes this job's id (resolved at runtime via `is_internal`, not a hardcoded UUID) and the job is filtered out of the regular job list so it isn't offered twice. **notify v11** does the same for email-scanner expenses that don't match a job. It stays `is_internal` so "projects completed" reads 6, not 7, and it never enters the hours-forecast sample. Backup of the moved rows: `company-expenses-backup.json` in the session scratchpad — revert with `update finance_entries set job_id=null where id in (…)`.
- **Hours forecast (`lib/forecast.ts`):** learned at runtime, not hardcoded — Σhours ÷ Σmodules per job_type over complete jobs. **As of 2026-08-05 R&R has only n=3** (2.05 / 1.39 / 1.89 h per module → 1.80 avg, range 1.39–2.05) and Reinstall n=1 (1.24). The UI ALWAYS shows the range and sample count; with n=1 it hides the range entirely and says "treat as rough". A type borrows the R&R coefficient only when it has NO finished jobs — borrowing on a merely-thin sample was worse, since a Reinstall-only job costed at the R&R rate overstates by ~45%.
- **Customer photos:** `customers.photo_path` → the EXISTING private `job-photos` bucket under a `customers/` prefix (no new bucket/policies). Picked via expo-image-picker with `allowsEditing` + 1:1 aspect, so the OS crop does the sizing — there is no image-processing library in the bundle. `components/CustomerAvatar.tsx` falls back to coloured initials.

- **Splitting one payment across jobs (2026-08-05):** a single ACH/direct deposit routinely pays several invoices at once, and the email scanner can only tag it to one job (or the company bucket when it matches none). `lib/documents.ts::splitPayment` divides an existing payment into per-job legs with hand-entered amounts — deliberately hand-entered, because a deposit doesn't always match an invoice to the penny. UI is the branch icon on any payment row in `JobInvoices`: job chips + amount per row, a live "assigned / left" readout, and it refuses to allocate more than the payment. **It reuses the ORIGINAL row for the first leg** so the scanner's `extracted.gmail_message_id` dedup survives and a re-sent bank alert still can't double-log the deposit. Unallocated money becomes an explicit "unassigned remainder" row on the original job rather than silently disappearing. NOT atomic (no multi-statement transactions over REST): legs insert first, the original is reduced last, so a mid-way failure over-counts visibly instead of losing money.
- **First real split (2026-08-05):** the $12,110 Chase deposit was divided into DC-26010 $6,650 + DC-26011 $2,310 + DC-26012 $3,150 — all three now balance to $0. Done in SQL before the UI existed; backup of the original row is `deposit-backup.json` in that session's scratchpad.

- **Login screen (2026-08-05):** background is **Devon's own Gemini-generated video**, converted to a looping animated WebP — `assets/images/login-solarflow.webp` (**720×1150 native, 12 fps, 8 s loop, 96 frames, 7.1 MB** — Devon explicitly accepted the download size for the quality). Real video playback would need a native player (expo-video) and therefore a full App Store build, so this route keeps the login shippable over the air. `assets/images/login-solarflow.jpg` sits underneath as the first frame: it paints instantly and is what shows if the animation ever fails to decode. Rendered with **expo-image** — React Native's own `Image` does NOT animate WebP.
  - **The source clip does not loop** (last frame differs from the first by 24.3 vs 10.0 for a normal 0.3 s step), so the asset is built as a **ping-pong**: 4 s forward + the same reversed. Seamless by construction. If you ever re-cut it, keep that or the login will visibly jump.
  - The Gemini watermark in the bottom-right of the source is **cropped off** (`crop=720:1150`) before scaling.
  - The wordmark is solid black on transparent, so the warm bloom behind it is **composited into every frame** by ffmpeg from `bloom_overlay.png`, matching the still fallback. Overlay is `bloom_720.png` at native crop size. Placement fractions: wordmark ≈0.33, verse ≈0.41–0.47 of screen height (cover-crop is horizontal on a phone, so image y maps 1:1 to screen y). Move the login layout vertically and the bloom must be re-rendered.
  - Conversion needs ffmpeg, which is NOT on this Mac by default — `python3 -m pip install --user imageio-ffmpeg` provides a self-contained binary.
  - WebP is a poor codec for this content — 7.1 MB buys 8 s at 720p/12 fps, where the original MP4 is 2.55 MB for 10 s at 720p/**24** fps. If a native build is ever being spent anyway, switching to `expo-video` with the raw MP4 would be both better looking AND a smaller download.
  - The earlier code-drawn particle grid (`components/SolarFlow.tsx`) was **deleted** — the video is the particle grid now. It's in git history if ever wanted.

- **WEB-ONLY job board (2026-08-05):** app.dcsolarkc.com renders the Pipeline as a stage-column board (`components/PipelineBoard.tsx`) — one column per stage in `STAGES` order, Pending Estimate far left through Complete far right, each with a live count and (for admins) the column's invoiced total. **iOS is deliberately unchanged** and must stay that way: `(tabs)/pipeline.tsx` gates on `Platform.OS === 'web' && width >= 900`, so the phone keeps its two-page swipe cards and a narrow browser window falls back to the same list.
  - Admins move a job between stages with the ‹ › buttons on a card, which call the new `lib/jobs.ts::updateJobStage` — it writes ONLY stage/status/completed_on, unlike `updateJob` which needs every editable field. Drag-and-drop was skipped on purpose: doing it properly across RN-Web and touch is a lot of fragile surface for the same outcome.
  - `PropertyArt` now takes an optional `scrim` (default 0.62, unchanged for the phone); the board passes 0.8 because its type is smaller and denser.
  - **Verifying web-only screens locally:** the pipeline sits behind auth, so build with `npx expo export --platform web` and serve `dist` with a clean-URL rewrite (`/pipeline` → `pipeline.html`) or expo-router 404s — plain `python3 -m http.server` is not enough. A throwaway route under `src/app/` rendering the component against `MOCK_JOBS` is the quickest way to eyeball a gated screen; delete it before committing.

- **Four rotating crew characters (2026-08-05):** `assets/images/installer-{a,b,c,d}-{0,1,2}.jpg` — (a) the original with a red impact driver, (b) a Black installer with long dreadlocks and a yellow/black driver, (c) a worker in a straw sun hat hammering a nail while holding a beer, (d) a ginger, slightly heavier installer in sport slides. 12 frames, 516 KB total. **Which character shows is derived from the clock** — `Math.floor(Date.now() / 2h) % 4` — so it rotates every two hours and the whole company sees the same one at the same time instead of a random pick per device; a 60 s interval catches the changeover without a relaunch.
  - Branded tools/clothing are drawn **generically** (a yellow-and-black driver, a plain amber bottle, plain black slides) rather than reproducing any logo or trade dress.
  - Generation gotchas worth knowing: asking to "raise the hammer" produced a SECOND hammer and a third arm — the fix was to state "exactly ONE hammer and exactly TWO hands"; and the ginger character first came back with **four legs**, fixed by spelling out "exactly TWO legs and TWO feet". Always eyeball every frame before committing.
- **Login backdrop fit (2026-08-05):** the login art is portrait (720×1150) and was hard-coded to `cover`, which on a desktop browser magnified it until only a narrow vertical strip showed. `src/app/index.tsx` now picks `contain` once the window is wider than the artwork and `cover` otherwise, so a phone still fills edge-to-edge while app.dcsolarkc.com shows the whole composition centred on the navy page colour. The first-frame JPEG is now expo-image's `placeholder` rather than a separate RN `<Image>` — RN-Web laid that one out at its natural 768×1376 anchored top-left instead of filling, which was invisible only because the animation used to cover it.

- **Estimates total = NEWEST estimate per job (clarified + fixed 2026-08-06):** deleting an estimate appeared not to move the Financials "Estimates" tile. It isn't a refresh bug — the tile has always counted only each job's most recent estimate, so deleting a superseded revision correctly changes nothing (verified live: adding a $1,234.56 estimate to DC-26003 *lowered* the total by $5,147.70 because it replaced a $6,382.26 one; deleting it restored the figure exactly). Devon chose to keep that meaning — three revisions of one job shouldn't total three times its value — so the count is now shown instead: the tile reads "newest of N on M jobs" and a pipeline card says "Est is the newest of N estimates" when a job has more than one.
  - **Real bug fixed alongside it:** the newest-estimate comparison used `occurred_on` only, and `fetchFinanceEntries` has no `ORDER BY` — so two estimates dated the SAME day (revising one the same afternoon) resolved to whichever row PostgREST returned last, and the company total could flip between refreshes with no data change. `lib/pipeline.ts::isNewerEstimate` now breaks ties on `created_at`, and both `fetchFinanceEntries` and `lib/financials.ts` select that column. Asserted deterministic against reversed row order.

## Security model (reworked 2026-08-06 — read before touching auth or RLS)

- **RLS is the ONLY real barrier. `role?.isAdmin` in the UI is cosmetic.** PostgreSQL ORs permissive policies together, so one broad policy defeats every narrow one on the same table. `finance_entries` and `employee_hours` each had an admin policy AND a blanket "company read" policy — meaning **any signed-in crew member could read all company money and everyone's pay rates straight from the REST API**. Measured before the fix: 4 viewer accounts could see 110 finance rows ($56,617.26 of payments) and 51 hours rows exposing 6 pay rates. Migration `2026-08-06_rls_money_lockdown.sql` drops both. Verified by impersonating a viewer (`set local role authenticated` + jwt claims): finance 110→**0**, hours 51→**5** (own only), jobs and customers unchanged. **`jobs company read` and `customers company read` were kept on purpose** — crew need those for field work.
- **`employees` is admin-assigned only, structurally.** RLS is enabled with **zero** insert/update/delete policies, so no client key can ever write to it — only the service role (dashboard). Signing up therefore cannot grant staff access whatever the client sends.
- **Everyone who self-signs-up is a CUSTOMER** (`2026-08-06_customer_accounts.sql`). A `SECURITY DEFINER` trigger on `auth.users` files any new user without a matching `employees` row into `customer_accounts`. Verified both ways in a rolled-back transaction, including an UPPERCASE staff email to confirm the match is case-insensitive. Customers currently have access to nothing: their only policy is reading their own `customer_accounts` row.
- **Routing vs security:** `lib/account.ts::getAccountInfo` returns `employee | customer | none | unknown` and is used for ROUTING ONLY. **`unknown` means a session exists but classification failed (usually no signal), and the tabs gate FAILS OPEN on it** — the crew work in dead zones, and bouncing someone to the login screen mid-job while holding a valid session would be a worse bug than a customer briefly seeing empty screens. RLS decides what actually loads either way. `(tabs)/_layout.tsx` now gates on it — before 2026-08-06 the tab routes had **no gate at all**, so anyone signed in (or typing /pipeline on web) reached the crew shell.
- **Account deletion** (App Store 5.1.1(v)): edge function `delete-account` (verify_jwt ON, service role, takes the user id from the verified JWT and requires `{"confirm":"DELETE"}`). Offered on the customer screen and in More. It deletes the LOGIN, not the `employees` roster row — that's Devon's employment record and the jobs/hours/finance history reference it.
- **Password policy (Supabase Pro, enabled 2026-08-06):** min 10, requires upper + lower + digit, **leaked-password (HIBP) check ON**, email OTP expiry cut 60 min → 10 min. HIBP needs Pro; the other three work on Free.
- **Session tokens are NOT httpOnly** — `localStorage` on web, `AsyncStorage` on native. That's the Supabase default and fine today, but it means XSS equals account takeover: be careful adding third-party scripts to the web app, especially once a public marketing portal shares the origin.
- **Two-factor for STAFF (2026-08-06):** `lib/mfa.ts` + `app/security.tsx` (More → Security & 2FA), with the login challenge in `app/index.tsx`. Password + TOTP. **No "remember me" toggle was needed** — verifying raises the SESSION to aal2 and it stays there until sign-out, so it's once per device already. QR renders on web; on the phone the setup key is shown selectable (`expo-clipboard` is NOT installed and would force a native build). An abandoned enrolment is cleared on the next attempt, so a half-finished setup can never lock anyone out.
  - **The break-glass came first.** `supabase/recovery/README.md` documents un-enrolling a lost authenticator via the service role, and it was tested against `auth.mfa_factors` BEFORE enrolment was offered to anyone. Never ship a lock you don't already hold the key to.
  - **Deliberate split:** staff get password + TOTP and NOT OAuth; customers get Google/Outlook OAuth and NOT 2FA. Stacking TOTP on an OAuth login is a good way to strand someone, and customers only ever see their own invoices.
- **Still open:** Google/Outlook OAuth for customers is not yet enabled (Google is a config change; **Microsoft needs an Entra app registration that DEVON creates — it is not about whether his customers use Entra**, a common misreading). Rate limits are per-IP only with no per-account lockout.

## Customer portal (2026-08-06)

- **Invite → link → read.** Devon taps **Invite** on a customer in More → Customers. That calls the `invite-customer` edge function (admin-only, service role), which uses Supabase's admin invite and stamps `customer_id` into the new user's metadata. The `handle_new_auth_user` trigger reads it, so the account arrives **already linked** to the CRM record. Linking at invite time is deliberate: a customer never types who they are, so nobody can claim someone else's projects by guessing an email. The button reads "In portal" once a login exists.
- **What a customer may see is decided in SQL, not the UI:** `my_projects()`, `my_documents()`, `my_balance()` — all SECURITY DEFINER, granted to `authenticated` only, revoked from `anon`. Customers have **no direct table access** to `jobs` or `finance_entries` at all; the functions choose the rows AND the columns, so a customer can't widen the query.
- **Expenses are excluded server-side** (`type in ('invoice','estimate','payment')`). That is our cost base — a homeowner must never see what we paid for their materials. Do not "fix" this by filtering in the client.
- Verified by standing up a throwaway linked login in a rolled-back transaction: it saw **10 of 27** jobs, **21 of 110** finance rows and **0 of 57** expenses.
- 12 of 16 customers have an email today; the other 4 need one before they can be invited (the function says so rather than failing silently).
- Customers are **web-first** by intent, but nothing blocks iOS — the same screens work once the app ships.

- **Offline / weak-signal banner (2026-08-06):** `lib/connection.ts` + `components/ConnectionBanner.tsx`, mounted in the ROOT layout so it floats over every screen. Three states — online / slow (>3.5 s) / offline — decided by polling `/auth/v1/health` (20 s when healthy, 6 s when not, plus an immediate re-check when the app foregrounds and on the web `online`/`offline` events). **No netinfo dependency on purpose**: that's native, and it would have cost a full App Store build instead of an OTA. It also measures the thing that matters — can we reach OUR server — since a phone can show four bars and still not reach Supabase. The copy names what actually survives: clock in/out is saved on the phone and syncs later, everything else does not. It's a banner, not a modal, because an installer up a ladder shouldn't have to dismiss a dialog to read an address.

## Notifications (added 2026-07-24 evening)

- **Local job reminders** (shipped): `lib/notifications.ts` schedules on-device notifications 24h and 1h before each job_schedule_dates start (next 14 days, default 8:00 AM when start_time is null). Re-synced on every Today-screen load; deduped via data.type tag. No server needed.
- **Verse of the day** (shipped): `lib/verses.ts` (42 public-domain-phrasing verses about work), shown on the sign-in screen instead of the old tagline; rotates daily, same verse for the whole crew.
- **Push token registration** (shipped, dormant): devices upsert Expo push tokens into `push_tokens` (migration 9) on sign-in. Nothing sends pushes yet.
- **Email-triggered pushes** (BUILT 2026-07-27, session 3 — awaiting Devon's one-time setup): code is in the repo — Edge Function `supabase/functions/notify/index.ts` (shared-secret auth, accepts direct/{from,subject,snippet}/db-webhook payloads, pushes to ADMINS by default via push_tokens + Expo push API) and Gmail poller `docs/gmail-notify.gs`. **Full setup checklist: `docs/NOTIFICATIONS_SETUP.md`** — APNs key (`npx eas-cli credentials -p ios`, interactive), deploy function + NOTIFY_SECRET in the dashboard, Gmail labels/filters, Apps Script + 5-min trigger, optional finance_entries INSERT webhook.
- **Chase bank alerts** (Devon enabled deposits 2026-07-27; purchase/withdrawal alerts = Devon's next Chase visit): business alerts email devon@dcsolarkc.com — deposits AND (once enabled) debit purchases, ACH withdrawals, wires, checks. All ride the same Gmail-filter pipeline (filter Chase's sender → DC-Notify label). Money-word subjects get a 💰 push. Bank alerts push to admins only.
- **Email → finance_entries transaction logging (BUILT 2026-08-03, Mac session — needs deploy + Apps Script repaste + backfill):** the `notify` function now parses every Gmail-shape payload for a transaction — dollar amount (balance-adjacent amounts excluded) + in/out keywords (deposit/remittance/"payment received"/"has paid" → type `payment` direction `in`; purchase/debit card/withdrawal/"check cleared"/"payment to" → `expense`/`out`) — and inserts a finance_entries row via the service role: description = subject, counterparty = sender name, occurred_on = email date, job auto-matched by DC-26### in the text or a unique job-name substring (else company-level, job_id null). Tagged `extracted: {source:'email-scanner', gmail_message_id, keyword, matched_job}`; deduped on gmail_message_id so re-sends are safe. The finance_entries INSERT webhook branch SKIPS email-scanner payments (email path already pushed — no doubles). Ambiguous emails (no amount / no keyword, e.g. "estimate accepted") push but don't log. `docs/gmail-notify.gs` now forwards messageId/date/body(4000 chars) — Devon must repaste it over the old Code.gs — and gained `backfillDcNotify()`: replays all DC-Notify mail since 2026-07-24 with `backfill:true` (logs entries, sends NO pushes, safe to re-run; per-email results in the Apps Script execution log). LIVE since 2026-08-03: notify v8 deployed, gmail-notify.gs repasted, backfill run (13 entries logged; first attempt failed because the OLD function was still deployed — it push-spammed on backfill payloads and logged nothing; redeploying fixed it). Wrong classifications are fixable in the app's finance edit UI, which now includes a job picker per expense row. **Payment auto-dedup (v8):** before inserting a payment, the scanner skips if ANY payment of the same amount exists within ±3 days (covers Devon manually logging a payment before the bank email lands). Expenses are exempt — identical purchases days apart can be real. The 7/28 Chase $4,715 deposit and Devon's manual 7/29 DC-26013 entry were merged by hand (manual deleted, scanner entry tagged to DC-26013 + counterparty Cromwell Environmental).

## State / near-term TODO

The live list is in **START HERE** at the top (open items for Devon, unverified surfaces, unclaimed follow-ups). Longer-range ideas still on the table, none started:

- Receipts → reimbursement report; Gusto data into the Employees screen; migrate the home-PC CRM (Cloudflare tunnel) into Supabase and retire it (PLAN.md Phase 5).
- Appointment reminders and any customer-facing automatic texting/emailing (docs/AUTOMATION.md — needs Devon's approval of wording, timing and consent rules first).
- Team-wide email visibility (today each admin sees only their own mapped mailbox), a third mapped mailbox (one line in both `MAILBOXES` maps), outbound attachments.
- Google/Outlook OAuth for the customer portal (Microsoft needs an Entra app registration by Devon).
- Property artwork: keys are set and generation works; remaining jobs are generated from each job's "Property artwork" card as Devon gets to them.
- Cosmetic: app icon is the skyline wordmark on cream; Android untouched.

## Conventions for future sessions

- Match the existing visual language: theme tokens in `app/src/constants/theme.ts`, cards + chips + pills, no new UI libraries. The palette was widened 2026-08-04 — the brand core (cream/sun/ocean/ink) is unchanged, with an accent ramp (teal/indigo/violet/coral/rose/amber/lime/mint/slate, each in `<name>` / `<name>Soft` / `<name>Deep`) plus `accentCycle`, `artPalettes` and `artGreens`. **Only ever ADD keys to theme.ts** — screens across the app reference them by name.
- Every data fetch degrades gracefully (mock data or friendly message) — never crash on RLS/missing-table errors; demo mode must keep working signed-out.
- New DB changes = new file in `supabase/migrations/` (idempotent: IF NOT EXISTS / drop-policy-then-create), and Devon pastes it into the SQL Editor. Update the status line at the top of this file after.
- Verify with `npx tsc --noEmit` + `npx expo export --platform web` before every EAS build.
